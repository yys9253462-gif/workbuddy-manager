# 更新日志

本项目的所有重要变更都会记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

> **这份文件是写给用户看的**，不是开发笔记 —— Release 页面与应用内「更新日志」页
> 展示的都是这里的原文。写的时候：
>
> - 讲**用户会遇到什么、升级后有什么变化**；不写函数名、模块路径、变量名、测试名、
>   调试过程，也不写代码评审式的取舍推演。
> - 引用报错时**照抄界面上那句原文**（用户就是拿它来搜的），但不必解释它是怎么产生的。
> - 环境变量、配置项、命令可以写 —— 那是用户要动手的东西。
> - 「为什么这么设计」只保留**影响用户决策**的部分（如「上限不再跟随上游配置，
>   改用这个环境变量」）；内部权衡留给代码注释与评审记录。
>
> 判据：**一个只用产品、不读代码的人，能否看懂这条对他有什么影响。**
> 详见 `docs/release-process.md` 的「更新日志的写法」。

---

## [未发布]

### 新增

- **请求日志能看到「这次请求有没有真的命中提示词缓存」了（用户反馈）**：Token 那一列
  后面多了一个小标记——命中为主时显示绿色的「缓存 98%」，完全没命中时显示琥珀色的
  「未命中」，鼠标悬停可以看到具体的命中 / 未命中 token 数。点开某条日志的详情，
  还能看到**命中、未命中、写入**三段 token 各自是多少。

  同一段前缀反复发送时，命中缓存的那部分便宜得多，所以这个数字直接反映成本有没有降下来；
  前缀缓存又是**按账号**存的，换了账号或改了前缀导致缓存失效时，配合左边的账号列
  就能看清是哪种。

  注意：**老版本的上游不返回这三个字段**，这时标记不显示（详情里写「未采集」）——
  这跟「没有命中」是两回事，不会用 0 冒充数据。

- **每个账号可以单独做成长任务了（用户反馈）**：「任务」页的「做任务 / 领奖」只能
  一次对全部账号执行——账号一多就要等很久（每个动作之间要留间隔），而实际常常只是想
  给某一个号补一轮。现在账号列表每行多了一个「活动任务」按钮，可以只对这一个账号执行：
  先「预览」看清能领到什么，再决定「一键领奖」还是「做任务」。

- **镜像构建可以在国内网络下跑完了**：镜像里有两步固定从国外取文件——装 docker CLI
  要从 `download.docker.com` 下静态包，装 compose 插件要从 GitHub Releases 下
  （容器内「重载上游 / 读上游日志 / 一键更新」依赖这两个）。国内直连会长时间卡在
  这两步甚至失败。现在构建时可以换成国内镜像与加速前缀：

  ```bash
  docker compose build \
    --build-arg DOCKER_CLI_BASE=https://mirrors.aliyun.com/docker-ce \
    --build-arg COMPOSE_URL_PREFIX=https://ghfast.top/
  ```

  也可以直接填进 `docker-compose.yml` 的 `build.args`。不填即原来的官方源，
  行为与改动前完全一致。

- **容器部署也能挂到子路径了**：域名根路径被别的站点占用、要把管理端放在
  `https://example.com/workbuddy-manager/` 这类子路径下时，用 Docker 部署此前只能
  手工改前端再重新编译。现在构建时传 `--build-arg BASE_PATH=/你的前缀`（或在
  `docker-compose.yml` 的 `build.args` 里填）就行，取值与 `WB_BASE_PATH` 一致。

- **仪表盘的到期积分可以「按天」看了（用户反馈）**：设置页新增一个开关
  「**到期积分按天模糊统计**」。开启后，仪表盘上那个数字不再是「最近一笔到期
  多少」，而是「最近**这一天**共到期多少」——同一天到期的多笔会合并成一条显示
  总和。

  以前的样子：总额看着不少，但最近一笔只有几百分，看不出「**这一天**有一千多分
  要作废」。开着它更容易判断该什么时候去把积分花掉。

  三点说明：

  - 这是**界面偏好**，只存在你自己的浏览器里（不影响别人，也不写进上游配置），
    所以它没有和下面那些带保存按钮的配置放在一起。**默认关闭**——升级后你看到的
    还是原来那个数字，想按天看再打开。
  - 合并按**本地日历日**分，不是「相隔 24 小时」。倒计时取那一天**最早**的到期
    时刻：宁可保守，免得你按「还有 12 天」安排，而实际当天凌晨就作废了。
  - 鼠标悬停时仍然看得到每一笔的金额与日期，不受这个开关影响。

- **请求日志能看出「这次调用落在哪个账号上」了（issue #69）**：日志表在「模型」
  前面多了一列**账号**，形如 `张叔叔(299e342b)`——昵称给人看，括号里的 uid 便于
  与账号页对照。同一把密钥的连续请求是一直落在同一个号上、还是被分散到几个号上，
  一眼就能看出来。

  这一列有两点要知情：

  - **账号是稍后补上的**，不是请求一结束就有：账号由上游决定、也不在响应里回传，
    本面板是读上游的容器日志再按时间对回去的，所以刚打完的请求可能还是「—」，
    十几秒后刷新即可。
  - **只有容器部署才有这份数据**（要拿容器日志的时间戳才能对得上）。上游不在本机、
    没挂 `docker.sock`、或用的是原生部署时，这一列会一直是「—」，其余功能不受影响。

### 修复

- **已经签到的账号不会再让你重复签到了（用户反馈）**：签到是按天算的，但面板此前
  看不出哪个号今天已经签过——于是同一个号一天之内被点了十几次，每次都会真的发一次
  请求；签到记录也被这一串「今日已签到」刷满，真正的失败记录反而被挤到看不见。

  现在账号列表会标出今天是否已签到（已签到的号是绿色勾、签到按钮不可点），
  「全部签到」按钮会带上今天还没签的账号数、都签完时变成「今日已签」。已签到的账号
  即使被点到也不会再发起请求，也不会再往签到记录里写一条。

- **子路径部署不再挑反向代理的写法**：之前要求反向代理用**带尾斜杠**的
  `proxy_pass` 把前缀剥掉再转发；若代理是原样转发（不少面板新建反向代理时给出的
  默认写法），表现为**页面能打开、点什么都 404**。现在两种配置都能正常工作，
  nginx 侧不需要改。

---

## [1.0.66] - 2026-09-23

### 新增

- **账号可以写备注了（用户反馈）**：用手机号邀请注册的账号，昵称往往认不出是谁，
  想删一个号时不知道该删哪个。现在账号列表每行有一个备注按钮，写一句只有你看得懂的
  名字（比如「张叔叔」「备用号」），备注会显示在昵称下面。

  备注存在本面板自己的库里、按账号绑定，所以**临时停用（改文件名）之后还在**；
  留空保存就是清除。已有的备注用琥珀色图标标出来，一眼能看出哪些号还没写。

### 修复

- **密钥列表的「版本」与「有效期」两列不再错位（用户反馈）**：加「版本」那一列时，
  表头插到了正确位置、单元格却留在了「有效期」后面，于是这两列的内容互换了——
  「版本」列下显示的是有效期，「有效期」列下显示的是版本。现在单元格顺序与表头一致。

---

## [1.0.65] - 2026-09-22

### 修复

- **「刷新 Token」重载失败时不再误报成功（社区贡献）**：令牌续期本身成功、但上游重载
  失败时，界面此前照样提示「上游已重载生效」——而新令牌其实还没被读进去，重启后可能
  又变回过期状态。现在会如实说「上游重载失败」并给出可执行的做法。

- **上游重启提示里的服务名写错了（社区贡献）**：无法自动重启上游时给出的那条命令写的是
  `docker compose restart wbapi`，而上游 compose 里的服务名是 `wb2api`（容器名才叫
  `workbuddy2api`）。照抄会得到「no such service」。

- **子路径部署的文档写清 `WB_BASE_PATH` 该写在哪（社区贡献）**：它必须加在 systemd unit
  的 `Environment=` 或 compose 的 `environment:` 里，**写进 `.env` 不生效**（服务端不读
  `.env`，只有 Windows 原生的启动脚本用 `--env-file`）。这个坑的症状很隐蔽：配置看着加了，
  但页面能打开、接口全 404，容易误判成反代写错了。

- **「全部签到」不再把国际版账号算进总数（用户反馈）**：账号池里混有国际版账号时，
  签到结果会显示成「5/6 个账号成功」并提示「部分失败」——但国际版**没有签到体系**
  （上游对这类账号直接跳过、不发请求），它既不会成功、也不是失败，只是被算进了分母。
  用户看到「少一个」会以为是漏签，反复去点；而那个号无论点多少次都是「已跳过」。

  现在它不计入总数，改为单独说明「另有 N 个账号不适用」；账号池全是国内版时口径不变。

## [1.0.64] - 2026-09-22

### 新增

- **账号被限流或冷却时可以「强制退出」（社区贡献）**：以前只能等它自己恢复，或者
  去容器里改上游的状态文件。现在账号页对处于冷却 / 模型级限流的账号多一个按钮：
  点一下会**重启上游容器**，清掉该账号的冷却、熔断、连败降权与模型级限流状态，
  其它账号与账号凭证不受影响。

  两点如实说明：它会真的重启一次上游（期间不可用，通常几十秒）；**如果根因还在，
  状态很快会再次出现**——这个按钮治的是「等不及」，不是「修好了」。

- **用量统计页新增「上游官方统计」（用户反馈）**：给上游那把 api_key（创建
  workbuddy2api 时拿到的，直连 7863）也想看到用量。这些调用不经过本面板，面板
  自己的统计里永远没有它们，所以现在把**上游自己那份**统计摆到页面上：累计请求 /
  成功 / 失败、Token、实付积分、缓存命中率，以及按模型的明细。

  口径写在段首：这份**包含直连上游的调用，且自上游进程启动以来累计**，与页面
  上方的时段筛选无关（别和按时段统计的数字混着看）。取不到时会写明原因（上游没
  起来 / 镜像较旧没有这个端点 / api_key 不一致），而不是显示成 0。

- **想用自己构建的镜像？fork 一下就行**：以前要拿到镜像只能等发版，或者自己装好
  Docker 与 Node 从头构建。现在 fork 本仓库后，把 `deploy/fork-image` 里的构建
  工作流放进 `.github/workflows/`，推一次代码就会自动构建 **amd64 与 arm64** 两份
  镜像并推送到你自己的镜像仓库，拉取地址形如
  `ghcr.io/<你的用户名>/workbuddy-manager-multiarch`。工作流**不需要改任何内容**，
  它按 fork 的实际仓库自动决定镜像归属与来源信息，构建完成后还会在运行摘要里直接
  给出可以粘贴的拉取命令。想同时推 Docker Hub 的话，在仓库里加两个 Secret 即自动
  启用。步骤见 `deploy/fork-image/README.md`。

  镜像名比上游多一个 `-multiarch` 后缀是**有意的**：上游那个包名可能已被一个未
  链接到本仓库的同名包占用，fork 对它没有写权限，推送会失败。

- **支持把管理端部署在子路径下**：以前管理端必须占域名根路径，根路径已被别的站点
  占用时就装不上。现在可以挂在 `https://example.com/workbuddy-manager/` 这类子路径下：
  构建前端产物时设 `NEXT_PUBLIC_BASE_PATH=/workbuddy-manager`，服务端设同值的
  `WB_BASE_PATH`，再用反向代理把该前缀剥掉转发即可。完整步骤见 `deploy/README.md`
  的「子路径部署」。

  两个变量都留空时行为与之前完全一致（根路径部署）。

  子路径部署后，网关接入地址会跟着变成 `https://example.com/workbuddy-manager/v1`；
  密钥页展示的地址会自动带上前缀，不用手工改。PWA 清单（`site.webmanifest`）里的
  图标改用相对路径，两种部署下都能正常加载。

### 修复

- **「自定义提示词文件」填成提示词正文，不再让上游起不来（用户反馈）**：这一栏要填
  的是**文件路径**，不是提示词正文。上游对它是硬校验——路径非空但读不到就拒绝启动
  （它有意不静默回落到内置默认），于是把正文粘进来会让上游容器反复重启、整个反代
  不可用，日志里是 `file name too long`。

  现在这样的值会在**保存时**被拒绝，并说明该怎么填：正文先写进一个文件，再填该文件
  **在上游容器内**的路径（例如 `/app/data/prompt-custom.txt`——上游默认把 `./data`
  挂到容器的 `/app/data`，把文件放进这个目录最省事）。顺带把「系统提示词模式」也
  校验了：填成 passthrough / custom / append 以外的值，上游同样会拒绝启动。

### 变更

- **账号被模型级限流时，直接写出是哪些模型（用户反馈）**：以前徽章只写「模型受限
  （2 个模型）」，看不出是哪两个——想换个模型、或者告诉调用方避开它们，都无从下手。
  现在直接列出模型名；超过两个时列前两个并带「+N」，鼠标停上去仍能看到每个模型
  各自的恢复时间与原因。

---

## [1.0.63] - 2026-09-22

### 修复

- **「复制链接」不再假报成功（用户反馈）**：点「复制链接」提示了「已复制到剪贴板」，
  但粘贴出来是空的。原因是复制失败时的**兜底方式**（浏览器不允许直接用剪贴板接口时
  走的老办法）没有检查自己的执行结果——被浏览器拒绝时它其实什么也没做，界面却照报
  成功。现在只有**确实写进剪贴板**才会提示成功；失败会如实说「复制失败」，并提示在
  链接上长按/选中后手动复制。

  顺带把这份复制逻辑合并成一份（原先两处各写了一遍，同一处缺陷存在两个副本）。


- **只读账号看到的积分与管理员不一致（用户反馈）**：同一批账号，管理员看到的是
  实时余额，只读账号看到的却可能是**上游快照值**——那数字是上游上次调度该账号时
  记下的，可能滞后数小时，于是出现「两个账号显示 0，实际有好几千分」，而 0 还会
  被按「余额耗尽」标红，看着像账号出了故障。

  原因是「刷新积分」这项查询此前只有管理员能用，只读账号的页面请求被拒后**静默
  回退**到快照值，界面上没有任何提示。现在：只读账号页面加载时也会拿到实时积分
  （与管理员同一份数字）；取不到实时值时会明确标出「**上游快照**」，且不再按
  「余额耗尽」标红——快照说 0 不等于确实没积分。「刷新积分」按钮仍只对管理员显示
  （它会强制查询所有账号，属于主动动作）。

- **更新上游时卡在「命令不存在: docker-compose」（用户反馈）**：在只支持
  `docker compose`（V2）的环境里，更新脚本会去执行旧的 `docker-compose`，报
  `命令不存在`（exit 127）后中断。这个毛病此前修过一次却没能到达用户手上——原因
  是面板自更新**不会替换更新脚本本身**，于是它一直停在旧版本，修过的问题不会生效。

  现在：更新日志会打印一行 `compose 探测：docker compose=…，docker-compose=…`
  与插件文件路径，失败时直接说明该做什么。**并且更新器本身以后会随包一起更新**——
  发布包在解压前已经验签，更新它与更新程序代码是同一性质的，不削弱那道防线；覆盖前
  会备份到数据目录。这一条正是为了让「更新器修过的毛病」能真正到达用户机器。
  （若你在手工维护 `deploy/`，设 `WB_SYNC_DEPLOY=0` 可保持原样。）

---

## [1.0.62] - 2026-09-21

### 变更

- **用量统计页默认显示「今日」（用户反馈）**：这一页上方是「今日 / 本周」的卡片，
  而下面的趋势图与「按模型」「按密钥」跟的是时段选择器——选择器原先默认「近 30 天」，
  同一屏里两种口径，很容易把 30 天的数字当成今天的。现在选择器加了「今日」并作为
  默认，三处一起跟随；想多看几天随时切「近 7 / 30 / 90 天」，切回去后图上的标识
  也会跟着变。

---

## [1.0.61] - 2026-09-21

### 修复

- **配了「模型映射」的别名现在能正常调用（用户反馈）**：密钥限定了版本（只允许国内版 /
  只允许国际版）时，用别名发起请求会被判成「版本不符」而拒绝——例如密钥限国际版、
  别名指向 `global:` 开头的模型，请求别名却报「该密钥仅限国际版模型，当前请求是国内版
  模型」。原因是版本判定发生在别名换算**之前**。现在按**实际发往上游的模型名**判定版本，
  与模型列表给出的结果一致。

  另外两处口径一并统一：模型白名单仍按**你发出的那个名字**判定（与模型列表一致——
  列表以别名下发，发别名就能过）；请求日志与用量统计里的版本归属改为按实际发往上游的
  模型名归档（此前按请求名，映射跨版本时会把一次调用记到另一栏）。**被拒绝的请求**也
  一样按实际要用的名字归档，所以按版本筛日志时不会看到归错栏的记录。

- **开了「账号管理接口」却仍走回退时，提示会说清差在哪（用户反馈）**：手改了上游配置里
  的开关、点「临时停用」却仍收到「该上游未启用管理接口」，用户无从判断问题出在哪。
  现在这种情况会直接说明：配置里**已开启**、但运行中的上游没有提供这组接口——上游只在
  启动时读这个开关，改完配置需要**重启上游容器**才生效（若已重启仍如此，说明上游镜像
  早于 2026-09-19）；提示里还会给上面板实际读到的配置文件路径，便于核对手改的是不是
  同一个文件。启用时提示只说「以后想去哪儿开这个开关」，不会再让你「重新停用」。

- **自动签到的「本轮汇总」行不再被版本筛选吃掉（用户反馈）**：签到记录页切到国内版 /
  国际版后，自动签到的轮次汇总行两个视图下都看不到——看起来像自动签到没跑。这行的
  账号列为空（它不属于任何账号，只汇总本轮结果），此前被当成「不属于该版本」筛掉了。
  现在这类不属于具体账号的记录（含脚本行）在所有版本视图下都显示。

  同一处的「共 N 条」也改成按当前版本统计（另外单独给出两版合计），不会再出现
  「共 6 条 · 仅显示最近 2 条」而列表只有 2 条的情况。

- **限流不再把被拒绝的请求也算进额度（用户反馈）**：每密钥限流此前把「刚被拒的请求」
  也计入窗口，于是客户端一旦以高于阈值的速率持续重试，计数永远降不回阈值以下——
  表现为「凑满一窗之后持续全拒、永不恢复」，限流实际变成了熔断。现在只有**放行**的
  请求占用额度：超出的仍返回 429，但把速率降下来能立刻恢复，持续高频则稳定放行到上限。

  阈值可用环境变量 `WB_GATEWAY_RATE_PER_MIN` 调整（默认每分钟 120 次，`0` = 不限制），
  本次也补进了 README 与 `.env.example` 的环境变量表（请求体上限 `WB_GATEWAY_MAX_BODY_MB`
  一并补上）。

- **把 `/dashboard/` 这类地址存成书签再打开，不再显示满屏原始数据（用户反馈）**：少数
  情况下（移动端 Safari 实测）客户端路由会退化成整页导航，跳到页面对应的数据文件
  （`/dashboard/index.txt`），浏览器于是把那份内部数据按纯文本渲染、地址栏也停在 `.txt`
  上，刷新只会继续看到它。现在这类「当页面打开」的请求会直接被送回对应页面；客户端
  自己的数据抓取不受影响。

- **账号积分耗尽时会直接说明（对齐上游新的错误分类）**：在「账号」页点「测试」时，
  积分用完的账号此前只显示「上游返回 code=14018 …」，看起来像账号坏了。现在会说明
  是**积分耗尽**（等签到恢复或换账号）。上游 2026-09-20 起把这个业务码明确归为积分
  耗尽，本端跟着翻译。

- **密钥的模型白名单现在会同时作用于模型列表（用户反馈）**：给密钥设了模型白名单
  （例如只允许 `global:deepseek-v4.1-flash`）后，客户端读到的模型列表仍然给出该
  版本的全部模型。而客户端普遍拿这个列表当模型选择器，于是用户会看到一个
  「能选、但一选就失败」的列表——挑白名单外的模型发起对话会报「模型不在密钥
  白名单内」。（版本限定这一维此前已经生效，只有白名单这一维漏了，两维口径不一致。）

  现在列表给出的就是这把密钥**真正能用**的模型。有几点边界也一并处理了：

  - 白名单里写**模型别名**（设置页「模型映射」里的名字）时，该别名会出现在列表里
    ——它确实是能调用的；此前按字面裁剪会让这类密钥的列表变空；
  - 白名单写 `global:xxx` 或 `cn:xxx` 两种前缀写法都认（与调用侧同一套判据）；
  - 白名单为空时行为不变（该版本全部模型）。

- **模型白名单填错会当场提示（用户反馈）**：白名单是文本框，填错了不会报错，只在
  客户端表现为「没有可用模型」——而空列表看不出原因（少打一个连字符？填成了显示
  名？）。现在在「密钥」页填写时会即时检查，把匹配不到模型的名字标出来。

  另外：模型列表与别名情况都读不到时**不做提示**（而不是把所有名字都标成错的），
  避免误导。

---

## [1.0.60] - 2026-09-20

### 新增

- **设置页新增「账号管理接口」开关（用户反馈）**：账号页的「临时停用」有两种做法，
  开关决定走哪一种：

  - **开启**（推荐）：停用只摘除转发流量，**签到与令牌保活照常执行**，积分与凭证
    保持活跃，随时可以启用回来；
  - **不开启**：停用会把账号完全移出账号池——不再转发，同时签到与保活也一并停止，
    停久了账号可能因令牌过期而失效。

  这个开关默认关闭（上游默认值）。此前它只能在容器里手改配置文件，所以绝大多数
  用户实际一直在用第一种做法而不知情——有用户升级后点「临时停用」，看到「账号将
  完全退出账号池」的提示才发现。现在可以在「设置 → 账号管理接口」里打开，
  保存后会自动重启上游使其生效。

  注意：开启要求上游已设置 `api_key`（上游要求管理接口必须鉴权，否则会拒绝启动），
  面板会在保存时拦住这种情况并说明原因。

### 变更

- **临时停用的提示里给出了下一步**：以前只说「该上游未启用管理接口，已改用改名
  方式」，用户不知道去哪儿开、只能来问。现在会直接指明开关位置与生效方式。

- **英文 / 日文 / 韩文 / 繁中界面里的漏翻**：设置页有一个字段的说明一直显示中文
  原文（「请求体上限（旧版上游）」及其说明）——短语表按整段文字匹配，那条拼接
  出来的说明漏了一个字符就再也匹配不上，于是静默退回中文，没有任何报错。
  已补齐，并加了机械检查，避免以后新增的说明再出现同样问题。

---

## [1.0.59] - 2026-09-20

### 修复

- **Codex 的补丁工具不再报错（社区贡献）**：以前通过 Responses 接口调用时，
  Codex 的 `apply_patch`（给文件打补丁的那个工具）会失败并提示
  `tool apply_patch invoked with incompatible payload`，补丁不会执行。

  原因是这类工具（自由文本工具）与普通工具的形状不同：它传的是一整段原始文本，
  而中间层按普通工具的 JSON 参数格式转发了。现在会按它本来的形状转发与还原。

- **分组声明的工具不再丢失**：有些客户端会把工具按分组声明
  （例如 `shell` 组下的 `read_file`）。以前这种分组里的工具一个都传不到上游，
  模型看不到也就不会调用它们。现在会展开后如常送达。

- **推理档位不再被改动**：通过 Anthropic 接口调用时，最高档（`max`）此前会被
  降一档发送，实际效果低于所选；上游支持的 `xhigh` 档则会被直接丢掉。
  这类请求现在按所选档位原样传达（各模型支持哪些档仍由上游按其能力归一）。

---

## [1.0.58] - 2026-09-20

### 修复

- **「刷新 Token」按钮现在真的会续期（用户反馈）**：以前点它只是让上游重载一次，
  并不会延长任何账号的有效期——按钮说的和做的不是一回事。现在它会直接向腾讯
  换取新的令牌并保存，界面会告诉你新令牌的有效期。

- **长期不用的账号不再因为令牌到期而失效（用户反馈）**：令牌续期原本只在两种
  情况下发生——上游到了保活时刻（默认每天一次），或者该账号正在被使用。
  长期闲置、或那一次保活恰好失败（网络抖动）的账号，会一路走到令牌过期，
  只能重新扫码。

  现在管理端会定时检查所有账号，有效期**不足 3 天**时自动续期。已经过期的账号
  也会尝试一次（刷新用的凭据通常比访问令牌活得久，多数情况下能救回来）。续期
  结果会记在「任务」页的令牌保活记录里，一眼能看出哪个号续期失败、原因是什么。

- **账号「临时停用」不再连带停掉签到与保活（用户反馈）**：以前的停用是让账号
  **完全退出账号池**——不再被选中转发，同时签到、令牌保活、猫猫旅行等任务也
  全部停止。积分不再增长、令牌不再续期，停久了账号可能已经过期。

  现在改为只在转发层面摘除该账号：**依然不被选中处理对话，但签到与保活照常
  执行**，积分和凭证都是活的。账号页会明确标出「已停用」，并说明任务仍在运行。

  如果你的上游版本较旧（没有相应的管理接口），面板会自动回退到原来的方式，
  并在提示里说清「此方式会一并停止签到与保活」。

- **任务页与日志页的时间现在能分清是哪一天（用户反馈）**：「近 24 小时」范围
  必然跨天，而列表里昨天的 14:03 和今天的 14:03 长得一模一样，扫一眼分不清
  哪条属于哪天。现在**不属于今天**的记录会标出「昨天」，今天的记录保持原样
  不变（逐行都标「今天」只是干扰）。

- **账号被限流时面板能看见了（用户反馈）**：腾讯对单个模型的频率限制是**按模型**
  生效的，账号本身仍然健康、换个模型就能继续用。以前这种状态下账号显示「在线」，
  完全看不出某个模型正在限流，只能进容器翻配置文件才知道。现在状态列会多一个
  「模型受限」标记，悬停能看到具体是哪些模型、预计什么时候恢复。

- **统计里的 token 用量不再恒为 0（用户反馈）**：通过 `/v1/responses` 流式接口
  调用时，界面记录下来的输入与输出 token 一直是 0，用量统计与费用都对不上。
  现在会记录真实数值。

- **流式响应不再少报输入 token（用户反馈）**：通过 `/v1/messages` 流式调用时，
  客户端从响应里读到的输入 token 恒为 0；使用提示词缓存时统计口径也不对，
  会出现重复计数。现在这两项都按协议正确回报。

- **接口返回的 token 估算不再低估中文**：`/v1/messages/count_tokens` 以前按
  「字符数 ÷ 3」估算，对中文大约是实际值的三分之一。客户端据此留的余量会少
  三倍，真实请求反而容易超长被拒。现在按文字种类分别估算，中文单独计算。

- **请求里设置的推理档位不再被忽略（用户反馈）**：通过 Anthropic 协议调用时，
  请求中的推理档位（thinking 预算 / output_config 档位）此前没有传给上游，
  用户设了「高」实际用的是默认档。现在会正确传达。

### 变更

- **失效账号的操作按钮不再「点了没反应」**：令牌无法再刷新的账号，点「刷新
  Token」会明确告诉你需要重新扫码或登录，而不是静默走一遍重载。

- **缓存键不再被丢弃（费用相关）**：通过 Responses 接口调用时，请求里带的
  `prompt_cache_key`（前缀缓存键）此前被丢掉了。上游支持这个字段，并且它的
  作用是复用提示词前缀缓存——同一段前缀带上它时命中率会显著提高，实测同一份
  8k token 前缀的扣费从约 0.34 降到约 0.02。现在会原样转达；你没带这个字段时
  仍由上游自行处理，行为不变。

---

## [1.0.57] - 2026-09-19

### 修复
- **点授权链接登录成功后，弹窗仍停在「等待」（用户反馈）**：在弹窗里点开授权
  链接、在浏览器中完成登录，切回管理端时界面不变，看起来像没生效——要等一会儿
  （最久一分钟）才刷新。

  原因是浏览器会**限制后台标签页的定时器**：你切去登录时，原页面进入后台，它的
  刷新被压低到大约每分钟一次。切回来后界面要等下一次刷新才更新。现在切回页面会
  **立即**刷新一次，登录结果马上出现。

  同一处的等待提示也确认了一遍：文案已说明可以点开链接在浏览器里登录（或扫码、
  手机号登录），不再是只讲「扫码」。

### 变更
- **统计页在长期使用后不再变慢**：总览与统计页每次刷新都要统计「今天」和「近 7 天」
  的失败数，而这里的查询方式让数据库无法走索引、每次都要翻一遍全部请求日志。
  日志是按天累积的，所以用得越久刷新越慢（实测 20 万条记录时单次约 48ms，
  而页面每 30 秒就会刷新一次）。

  查询方式已改为等价的索引写法（同一份数据下逐条比对过，数字完全一致）：
  同样的统计现在约 10ms，快了约 4 倍，并且数据越多优势越明显。**你不会看到
  数字有任何变化，只是页面更跟手了。**

## [1.0.56] - 2026-09-19

### 修复
- **从源码部署时 `docker build` 必然失败（用户反馈）**：`git pull` 之后执行
  `docker build .` 会报

  ```
  ERROR: failed to build: ... "/web/out": not found
  ```

  因为 `web/out`（前端产物）不入库，从源码部署的用户工作区里没有它。发布包
  用户不受影响（包里带着构建好的产物），而文档也没写明这个前提。

  现在镜像会自己处理：**有现成产物就直接用**（发布包与 CI 走这条），**没有就
  在容器内构建**（源码部署走这条）。所以 `git clone && docker compose up -d --build`
  一条命令跑通，不需要你先装 Node 或手动构建前端。

  国内网络下 npm 官方源可能慢，可加镜像：
  `docker compose build --build-arg NPM_REGISTRY=https://registry.npmmirror.com`

- **推理内容为空时不再写入空字段**：客户端发来的推理项如果**没有可用内容**
  （畸形输入），此前会挂一个空字符串。社区实测指出该字段为空时上游仍会拒，
  所以挂它把「没有痕迹」变成了「有痕迹但内容为空」——不但没帮上忙，反而更糟；
  而已有证据也表明不挂不会更差。现在改为不挂，并记一条日志（这条路径此前
  完全静默，真遇到对应报错时看不出是哪个客户端发了空推理项）。

  正常多轮不受影响：客户端带回的真实推理内容照常写入。

## [1.0.55] - 2026-09-18

### 变更
- **「IP 访问日志」只记录被拦截的请求**：那张表的用途是安全审计——回答「谁在扫我、
  谁被挡了」。此前连正常请求也逐条记录，实测线上 7877 行里只有 17 行是拦截记录
  （0.2%），真正的信号被淹没；而它有条数上限，被正常流量占满后**能回溯的时间
  从数月缩到十几天**，真出事时记录可能已经被挤掉了。

  现在默认只记拦截（表头也注明了），界面清爽很多。放行请求的明细一直都能在
  「请求日志」里查到，所以不丢信息。需要核对「某个 IP 到底来过什么」时，
  设 `WB_AUDIT_ALL_ACCESS=1` 可临时恢复全量记录。

  顺带修了一处口径不一致：走 `/v1/messages`（Claude 系客户端）的请求被拒绝时，
  此前**不会**出现在这张表里——只有走 `/v1/chat/completions` 的才记。现在两套
  协议的拦截记录一致。

- **DeepSeek 多轮对话的推理内容，改为同时带两个字段名**：针对社区报告的
  DeepSeek 多轮报错（`11155 reasoning_content_missing`），助手消息上的推理内容
  现在会同时写进 `reasoning` 与 `reasoning_content` 两个字段。

  社区报告称上游只认 `reasoning`、写另一个等于没写，但**我们没能复现该结论**——
  用真实账号直连腾讯试了 7 种构造，全部正常返回（含报告里说必然失败的那种）。
  既然说不准，就两个都带上：多一个上游不认的字段没有副作用，少一个却可能
  触发那个报错。

  因此这一版**不声称修好了 11155**。如果你的多轮对话仍在报它，请把客户端收到的
  完整报错、所用客户端与模型名贴到 issue 里，我们继续查。

### 新增
- **请求日志自动清理**：请求日志逐笔记录所有调用（每天上千条），此前没有任何清理
  机制，会一直增长。现在保留 90 天，更早的自动删除。

  注意：清理只影响**明细**，用量统计（按天汇总的数字）不受影响。但超过 90 天后，
  那段时期的明细不能再用来「重建统计」——这是刻意的取舍，九十天前的用量早已定稿，
  不值得为它永久保留明细。

- **统计里能看到失败请求了**：此前界面上的请求数、Token 消耗、趋势图全部来自
  用量汇总，而那份汇总**只统计成功的请求**——被拒绝的调用（密钥无效、配额用尽、
  限流）与「池里没有可用账号」这类失败都是零消耗，压根不进汇总。

  结果是：**出过事故的日子在图上看起来像「没有请求」**。有用户遇到凌晨全池中断、
  当天 31 次调用全部失败，而管理端趋势图上那天是空白 —— 只能去翻日志才知道出过事。

  现在：
  - 总览与统计页的卡片会提示「今天 N 次失败」（有失败时优先显示它）；
  - 趋势图多画一条红色虚线表示失败数，与请求量分开（两者来自不同数据源，
    不叠加）；
  - **只有失败、没有成功的那一天也会出现在图上**（此前会整条缺失），
    那正是最该被看到的一天。

  失败数只统计**带密钥**的请求：没带密钥的 401 是扫描器与配置错误的噪声，
  混进来会让这个数字失去信号价值。

## [1.0.54] - 2026-09-18

### 修复
- **Responses / Anthropic 协议下 DeepSeek 多轮对话仍报 11155（用户反馈）**：上一版
  声称修好了这个问题，但只修了一半——**没生效**，用 Codex 连着聊，第二轮起上游照样回

  ```
  400 code=11155 reasoning_content_missing
  the reasoning content from the previous turn must be passed back in thinking mode
  ```

  原因是：我们虽然能看懂客户端带回来的推理内容，但**从没给过客户端可以带回来的东西**。
  这两套协议里，客户端只回传**带凭据**的推理块——Responses 认 `encrypted_content`，
  Anthropic 认 `signature`。而我们的响应里这两个字段一个都没有，客户端于是在下一轮
  把推理整个丢掉，我们那半边解析代码等于白写。

  现在补上了：非流式与流式的响应都会带上凭据，客户端原样带回后我们能还原出完整的
  推理原文交给腾讯。**只有在你启用了扩展思考时才会返回思考块**（没启用时不返回，
  避免严格的客户端把多出来的内容块当成异常）。两套协议用同一套凭据编解码，所以
  同一轮对话在两套协议之间切换也认得。

  这一版补上了「第一轮拿到凭据 → 原样带回 → 第二轮确实带上了推理内容」的完整往返
  测试；上一版的自测只验证了「发出去的 body 对不对」，恰好绕过了凭据这一段。

- **任务页筛选到没有记录的类型后，筛选栏整条消失（用户反馈）**：在「自动任务与积分
  记录」下点到一个当前没有记录的类型（如「余额查询」），筛选栏会连同列表一起不见，
  只能刷新整个页面才能切回「全部」。

  原因是接口返回的汇总数字被**当前筛选**影响了：筛选栏要靠它显示各类型的条数，
  而它一归零，界面就以为「一条记录都没有」，把整条筛选栏收了起来。现在汇总数字
  始终按时间范围与版本统计，不受当前筛选影响。

- **「已拦截」不说是谁拦的（用户反馈）**：安全页的 IP 访问日志之前只写「已拦截」，
  看不出是哪一关拦的——「请求没带密钥」「密钥无法识别」「IP 规则拦的」在界面上长得
  一模一样，而处置方式完全不同（改客户端配置 / 重新发密钥 / 改 IP 规则）。有用户
  因此排查不下去来提问，但日志本身回答不了。

  现在每条拦截后面直接写明原因（如「API Key 无法识别」），并区分密钥层的各种情况
  （已停用 / 已过期 / 配额用尽 / 模型与密钥限定不符 / IP 白名单）。

  同时修正一处更误导的行为：被密钥层拒绝的请求（停用、过期、配额用尽）此前在日志里
  显示成**「已放行」**——照着这个去查网络和上游，方向完全跑偏。

- **国际版登录只提「扫码」，没说可以用 Google 登录**：添加账号的说明此前只讲扫码和
  手机号，于是想用 Google 登录国际版的用户以为做不到。其实那个授权链接本身就能在
  电脑浏览器里打开（界面上一直可点、可复制），页面上有哪些登录方式由腾讯决定。

  现在文案改为「点开链接在浏览器里完成授权」，国际版并注明可用 Google 等第三方账号。

## [1.0.53] - 2026-09-18

### 修复
- **Windows 启停脚本会喷一串「不是内部或外部命令」**：`deploy/windows-native/`
  下那两个模板脚本和根目录的 `start.cmd` 里，注释是中文的，而 Windows 的命令行
  按**系统本地区域编码**（中文系统是 GBK）读取批处理文件，于是中文注释被拆散、
  碎片被当成命令执行。表现是启动时刷出一串看不懂的报错（脚本后面其实照常运行，
  所以更容易误以为「装坏了」）。

  现在这些脚本的注释改为英文（纯 ASCII），并把换行符固定为 Windows 的 CRLF。
  上游自带的三个 Windows 脚本也是纯 ASCII——这不是洁癖，是批处理的硬约束。

## [1.0.52] - 2026-09-18

### 新增
- **积分到期倒计时**：腾讯的积分按套餐分批过期，过期即作废。此前界面只显示余额
  合计，看不出「这些积分什么时候没了」——容易攒着攒着就白白过期。

  现在每笔积分各算各的到期时间：积分旁显示最近一笔的额度与倒计时（如
  `300 · 8 天后到期`），悬停可看全部套餐的额度和具体到期时刻、以及合计；
  首页的积分卡片同样提示最近一笔的金额与到期日期。

  角标里带上额度是刻意的：只写「8 天后到期」容易被读成「这一列积分全都 8 天后
  过期」，而通常只有其中一小笔到期、大头还在后面。倒计时按紧迫度分三档颜色
  （1 天内红、7 天内橙、更远灰）。没有到期时间的套餐不显示倒计时，不会凭空多
  一个「已过期」。

### 修复
- **看不到「连败降权」，只显示成「冷却中」（用户反馈）**：上游对连续失败的账号会
  临时移出账号池（默认连续 5 次失败、移出 10 分钟），并把它算进**冷却**。而界面
  只显示「冷却中」，于是两类含义完全不同的情况混在一起——等一会儿就好的限流退避，
  与「这个号在持续失败」的降权，用户无从判断该等着还是该去查这个号。

  现在分开：账号列表标成「连败降权 · 剩余时间」（悬停会说明连续失败了几次、若反复
  出现该去检查令牌），首页「反代上游」面板新增一行「连败降权」计数，健康快照也按
  两类分别标注。判定以**截止时间是否在未来**为准，因此已经恢复的账号会正常显示
  「在线」，不会因为字段还在就一直标着降权。

- **国际版账号「测活」可能误报失败**：测活是面板直连腾讯确认账号可用，走的是
  与上游转发相同的聊天接口。而上游刚把国际版的接口路径从 `/console` 换成了
  `/v2`——原先那个端点上挂着腾讯云的 WAF 内容规则，消息里出现反引号 `printf`、
  `whoami` 这类命令执行特征会被拦成 403（用户只是问一句 shell 命令就中招）。

  现在测活也跟着走 `/v2`，与上游完全一致：测活说可用，就代表真的能用来对话。

## [1.0.51] - 2026-09-18

### 新增
- **Windows 原生部署支持（社区贡献 @dengfeng89，PR #32）**：此前的 Windows 支持只做到
  「管理端能跑起来」——账号管理的关键闭环仍然写死 `docker restart` / `docker logs`，
  在上游以**本地进程**（而非容器）运行时无法工作。

  现在新增 `WB2API_MODE=native`（默认仍是 `docker`，现有部署行为不变）：重启上游走
  可配置的启停脚本，上游日志从文件读取，并明确拒绝依赖 Linux/Docker 的网页一键更新
  （返回清晰提示，而不是执行到一半才失败）。部署步骤已写进 README。

  **上游的启停脚本要自备**：上游官方只提供 Docker 部署，没有 Windows 原生的启停
  脚本，所以我们补了一对可直接改用的模板（`deploy/windows-native/`），并把两个
  必须遵守的约定写清楚：启动脚本要立即返回（否则界面会等到超时才报失败，而上游
  其实已经起来）、日志要写到 `WB2API_LOG_FILE`（否则「任务记录」采集不到）。

### 修复
- **DeepSeek 多轮对话会卡死（社区反馈）**：用 Codex / Claude Code 这类客户端连续对话
  时，从第二轮起可能报「上一轮的推理内容必须回传」，随后请求反复失败，最终连不相干
  的模型也调不动了。

  原因在协议转换这一层：推理内容被我们**丢掉了**。腾讯要求 DeepSeek 系模型把上一轮
  的推理内容带回，上游本有一道补救（发现请求里带推理痕迹就给所有回复补上该字段），
  但痕迹被我们丢掉后，那道补救永远不触发 —— 于是每次请求都被腾讯拒，该账号被连续
  记为失败、触发降级冷却；池里账号陆续冷却后，任何模型都返回 503，客户端不停重试，
  又让冷却不断续期，形成死循环。

  现在把推理内容正确带回（Responses 与 Anthropic 两套协议都修了，字段名用上游认识的
  `reasoning_content`）。修复后的请求体已用上游真实的预处理逻辑验证：助手消息带上了
  该字段，腾讯的校验可通过；修复前则确认为空。

- **手机号登录容易「走到一半说失效」**：腾讯授权页除了扫码，也支持**手机号 + 短信
  验证码**登录。而我们的等待窗口只有 5 分钟——短信延迟或中途去找手机就超时，界面
  报「二维码已失效」，但用户明明刚授权成功。

  现在等待窗口放宽到 15 分钟（上游对此本就没有超时限制），并修正了几处只提「扫码」
  的文案：提示里明确说明「扫码或手机号登录均可」。同时把「二维码已失效」改为中性的
  「授权已失效」——用手机号登录的人从头到尾没见过二维码，看到那句会以为是别的问题。

## [1.0.50] - 2026-09-18

### 修复
- **用量页的「重建统计」报错，且「修复统计」可能也没反应**：两个按钮都会返回
  `Internal Server Error`。

  原因与**历史数据**有关：较早版本的调用记录里，版本归属（国内版 / 国际版）这一列
  是空的（该列是后加的），而重建与修复在归并这些记录时分组依据不一致，导致同一组
  数据被写两次、撞上唯一约束而失败。

  更麻烦的是重建的**顺序**：它会先清空统计表再重新写入，而中途失败时清空已经生效
  —— 于是统计表被清空或只写入一半，调用日志却完好。这解释了另一个现象：明明有调用
  记录，「今日请求」却显示为 0，而且每点一次重建就再破坏一次。

  两点修正：分组依据改为一致（含空值归一），重建改为**同一事务内完成**——
  中途失败会整体回滚，原有统计原样保留，不会再出现「点一下反而更糟」。

  如果你之前点过重建并遇到报错，升级后**再点一次**即可把统计补齐（调用日志一直是
  完整的，不会丢）。

- **添加账号时若恰逢写入，账号可能短暂掉线**：上游新版本开始每隔几秒自动重新
  读取账号目录（这样新增账号不必重启上游即可生效）。而本面板写入账号文件的方式
  在此之前不是"一次成型"的——写的过程中存在一个极短的空档，如果恰好在那一刻被
  读到，该账号会被上游当作"已删除"而暂时移出，随后又被写回。

  表现是账号偶发地短暂掉线，且日志里看不出原因。发生概率很低，但新版上游的读取
  是持续进行的，迟早会碰上。现在改为**原子写入**（先写临时文件、再一次性替换），
  读到的永远是完整内容：要么是旧版本、要么是新版本，不存在空白或半截。

- **部分部署下新加的账号可能读不到**：上一条的原子写入若按默认权限落地，会写成
  「只有写入者本人可读」。**宿主部署**下本面板以 root 写入、上游以另一个用户身份
  读取，于是账号加进去了却在池里找不到，且没有任何报错。现在显式设为其他用户也可读。
  同一账号被同时写入时（批量操作或连点重试）也不会再互相干扰。

### 适配上游
- **账号变更不再需要重启上游**：上游新增了账号目录的自动读取（每 5 秒一次），
  新增、删除、临时停用账号都会自动生效。本面板此前为此触发的重载**仍然保留**——
  对旧版上游那是唯一生效途径，对新版则省去最多 5 秒等待，两种版本行为一致。

## [1.0.49] - 2026-09-17

### 修复
- **「成长任务一键执行」的三个按钮都不能用（issue #31，v1.0.48 引入）**：升级到
  v1.0.48 后，任务记录页的「预览 / 一键领奖 / 做任务」点了会报
  「当前环境没有事件循环，无法后台执行」，每次必现。

  同一个缺陷还影响**定时领奖**：到点不会真的执行，只在日志里留下同样一句失败信息。
  如果你在用定时领奖，请尽快升级到本版。

  修复后三种模式都能正常触发，定时领奖也照常工作。

  排查已确认：脚本本身、上游连接、账号数据都没有问题，不需要重新配置或重新扫码
  —— 纯粹是 v1.0.48 引入的启动流程缺陷。

## [1.0.48] - 2026-09-17

### 适配上游
- **请求体上限不再跟随上游配置**：上游新版本移除了 `server.max_body_mb`（不再限制
  请求体大小）。我们此前会读这个键决定入站上限、读不到就按 8 MB 处理，于是升级上游
  后会出现一个**很难自查的现象**：较大的请求（长上下文、带附件的对话）被本网关以
  413 拒绝，而上游其实能正常处理；按提示去上游配置里找那个选项也找不到（它已经
  不存在了）。

  现在上限由本网关自己的配置决定（环境变量 `WB_GATEWAY_MAX_BODY_MB`，默认
  **32 MB**），报错信息也如实说明该改哪里。

  保留上限是必要的：本网关要把完整请求读进内存再解析，不设限时几个并发的大请求
  就能把内存吃满。所以做法是把默认值调到合理大小，而不是取消限制。

  兼容性：若你的上游还是旧版（配置里仍有那个键），本版继续尊重它的取值 ——
  两边先后升级都不会出现「一边允许 256MB、另一边 413」的错配。设置页对应字段
  已标注为「（旧版上游）」。

### 新增
- **API 密钥可按积分限额（issue #27）**：密钥除原有的 Token 额度外，新增「积分
  额度」。起因是实际使用中的问题：**按 Token 限额估不出花了多少积分** —— 同样
  1M Token，便宜模型与贵模型的扣费能差几十倍。

  额度按上游返回的**真实扣费**累计（与「用量统计」页同一份数据，不是估算）。两个
  额度各自独立、任一超限即拒绝，都留 0 表示不限；存量密钥升级后行为不变。界面显示
  已用 / 上限，超限提示里带具体数字。

  有一点需要知情：若上游没返回扣费信息（例如上游版本较旧），这类调用**不计入**
  额度，界面上用量会一直是 0 —— 那种情况请改用 Token 额度。

- **官方镜像部署不再需要手动准备任务脚本（issue #29）**：以前用官方**镜像**部署
  上游时，「一键执行」会提示找不到任务脚本，需要自己从镜像里把脚本导出来，而且
  每次上游更新都要重做一遍。现在会自动从上游容器里取，并在上游更新后自动更新；
  脚本缓存在 `data/upstream-scripts/`，随数据卷持久化。

  说明：镜像部署使用该功能仍需挂载 `docker.sock`（读取脚本本身就需要它）。若源码
  部署、挂载目录里已有脚本，则直接使用、不依赖 docker。

### 修复
- **一键更新上游时找不到 compose 命令（issue #28）**：容器镜像现已自带，不再依赖
  宿主机装没装；确实不可用时也会说明缺什么、怎么补，而不是只留下一句「重建失败」。
- **更新上游会覆盖你改过的 `docker-compose.yml`（issue #28）**：1Panel 等场景下给
  上游加的 `networks` 配置，更新后被覆盖会导致容器重建后网络丢失、直接失联。现在
  更新流程会保留你的定制并在更新后原样恢复，只重新施加端口收敛这一处安全基线。
- **`unable to open database file` 会说明原因与修法（issue #30）**：容器部署首次
  启动时若 `./data` 目录属主不对，以前只报这一行既没有路径也没有原因的 sqlite 错误。
  现在会指出具体路径、判断是目录还是文件不可写、给出对应的 `chown` 命令（改不了
  属主的网络文件系统另有命名卷写法）。compose 文件里对应位置也补了说明。
- **扫码确认后提示「二维码已失效」，但账号其实已授权成功（issue #26）**：写账号
  文件失败时（常见于目录权限不对），界面会把用户引向「重新扫码」，而重扫必然同样
  失败。现在会明确说明「已授权成功、是写文件失败、该查哪个目录、不需要重新扫码」，
  并且重试无需重新扫码。
- **扫码弹窗在偶发网络抖动时会中断（issue #26）**：以前几次零散的请求失败累加起来
  就会打断一次正常的扫码；现在只有**连续**失败才提示，偶发抖动会被忽略。
- **扫码弹窗把两种「失效」分开提示（issue #26）**：以前「二维码真的过期」与
  「服务端在等待期间重启过」显示的是同一句话，用户会一直重扫却怎么都不成功。
  现在两者分开说明，各自给出对应的下一步。

## [1.0.47] - 2026-09-17

### 安全
- **管理端登录会话加固**（来自用户反馈）：此前会话有效期 7 天，期间不轮换、不随
  活跃度重置，也无法主动作废。现在改为：

  1. **总时长 1 天**：登录后最多维持这么久，可用 `WB_SESSION_DAYS` 调整（想放宽就
     调大这个值，例如 30）。
  2. **空闲自动失效 + 活跃自动续期**（`WB_SESSION_IDLE_HOURS`，默认 12 小时）：
     一直在用就不会被打断，放着不管则会自动过期。续期只延长「最近活动」这一项，
     **不会延长总时长上限** —— 否则凭证一旦泄露，只要定期发个请求就能一直续下去。
  3. **一键「吊销所有会话」**（个人信息面板）：让已在别处登录的凭证立即全部失效，
     不必改密码（改密码会连带影响其它用途）。比「退出登录」彻底 —— 退出只清当前
     浏览器，吊销是让服务端把所有已签发的凭证作废。仅管理员可用，且只影响自己。

  另外补了一条**反代配置告警**：若你的站点走 HTTPS 反代，但反代没有透传协议头，
  会话 Cookie 会静默地不带 `Secure` 标记（你以为受保护，实际没有）。现在登录时
  检测到这种情况会在日志里明确告警，并给出两种修法（补反代配置，或显式设置
  `WB_SECURE_COOKIE=true`）。

  **升级后需要重新登录一次**（旧凭证无法判断活动时间，按保守策略作废）。

### 修复
- **任务记录里账号维度的日志越来越少，积分收益显示为 0**：上游新版本把日志里的
  账号标识从 `9b212d8c` 改成了 `猫猫(9b212d8c)`（带昵称，便于人工辨认），而我们的
  解析只认原来的纯 hex 形式，于是带昵称的行被整条丢弃 —— 不报错，只是记录悄悄
  变少。最要紧的是旅行领奖那行是积分收益的唯一来源，丢掉它会让收益一直显示 0
  （把「赚到了」显示成「没赚」）。现在新旧两种格式都能识别。

- **配置项设为 0 时会把管理员锁在门外**：`WB_SESSION_IDLE_HOURS=0` 本意是「关闭空闲
  判定」，实际效果却是「所有会话立刻失效」—— 登录看起来成功，下一次请求就被踢回
  登录页，而提示只说「长时间未操作」，完全看不出是配置问题。`WB_SESSION_DAYS=0`
  同理，会让登录后立刻过期。现在 0 按预期表示关闭 / 由下限钳制，不再产生这种自锁。

- **任务记录结果列在英文等界面下会显示内部键名**（如
  `tasks.creditLedger · 某某@example.com`），以及签到记录里若干固定文案没跟上
  界面语言。现在五语言都已补全；未收录的文案按原样显示原文，不会再露出键名。

### 新增
- **「成长任务一键执行」面板的日志支持多语言**：此前该面板回显的是上游脚本的原始
  输出，那批文案写死在上游仓库里，因此英文 / 繁体 / 日文 / 韩文界面下这一块全是
  中文。现在会跟随界面语言显示（动态内容如账号、任务码、计数、异常文本仍按原样
  保留，便于与上游日志对照）。上游改文案或新增日志行时，最坏是继续显示中文原文，
  不会出现空白或丢失。

  顺带修正：任务历史里的「一键执行」类型名、开始 / 停止提示不再固定中文。

## [1.0.46] - 2026-09-17

### 新增
- **临时停用账号（issue #21）**：账号页每行新增「停用 / 启用」按钮。停用后该账号
  不再被使用、也不会被选中 —— 某个号一直失败或在拖后腿时，先停用它比删掉更合适：
  删除会丢凭证、只能重新扫码，停用完全可逆。

  界面上会区分三种「不可用」：**「已停用」是你自己的操作**（灰字），「已禁用」是
  上游因连续失败自动停的（红字），「未加载」才是账号文件有问题。以前三者混在一起，
  刚停用的账号会被报成「账号文件可能有问题」，把主动操作说成故障。

### 修复
- **浏览器自动填充密码后登录必定失败（issue #22）**：浏览器自动填充不会触发页面的
  输入事件，导致界面显示有密码、实际提交的是空值，服务端只能回「用户名或密码错误」。
  用户会以为自己记错了密码，反复重试直到触发失败锁定（连续 5 次锁 10 分钟）。
  现在提交时直接读取输入框的实际内容，手输 / 自动填充 / 密码管理器三种情况都正常；
  真读到空值时也会提示「若使用了自动填充，请手动重新输入」。

### 适配上游
- **提示词模式新增 `append` 取值**：上游把该选项从两种扩到三种 —— 透传客户端
  system（默认）/ 替换为网关提示词 / **在客户端提示词之后追加网关提示词**（既有
  消息逐字不动）。我们的设置页此前只认前两种，用户填上游支持的新值会被面板拒掉
  （「上游支持、面板说不合法」）。

  这个方向必须跟紧：若面板放行了上游不认的值，会写进一份让上游**启动即失败**的
  配置，表现为「保存成功、然后上游起不来」，比当场报错难查得多。现已三值对齐。

  同期上游的其他改动**不影响使用**（已逐项核对）：请求记录相关的内部调整、
  账号池调度优化、会话与认证模块的并发修复等，都属于上游自身实现，不改变我们
  对外的行为。

## [1.0.45] - 2026-09-17

### 变更
- **模型名不再带 `cn:` 前缀，直接用腾讯自带的模型名**。`cn:` 是路由用的内部标记，
  不是模型名字的一部分；此前管理端把它原样显示出来，于是模型中心里是
  `cn:deepseek-v4.1-flash` 这种样子，用户照着去配客户端既难看、又让人以为必须
  带着前缀写。现在显示与复制出来的都是干净名字（`deepseek-v4.1-flash`、`glm-5.2`）。

  注意：**国际版模型的 `global:` 前缀必须保留**（去掉会被路由到国内账号池，调用
  会失败）。存量客户端写的裸名照旧可用，行为不变。

- **新建密钥不再让用户选版本，直接跟随当前所在页面**：在哪个版本的界面里创建，
  就是哪个版本的密钥，弹窗里只显示一个版本徽章与说明。此前的下拉框选错了不会
  当场提示，只有真正调用时才报「仅限某版本」。编辑已有密钥时仍保留选择器
  （存量密钥、尤其「不限制」的老密钥需要能改）。

- **模型白名单的新旧写法互认**：存量密钥的白名单里可能写着 `cn:glm-5.2`，而界面
  现在推荐裸名。若按字面比对，升级后会出现「原本能用的密钥突然报模型不在白名单里」，
  且报错看不出是前缀差异。现在两种写法都能匹配。

### 修复
- **「做任务」点了之后卡很久没有任何输出**（线上反馈）。两个独立原因，都让界面
  看起来像卡死、实际都在正常干活：

  1. **脚本的输出被缓冲住了**：非终端环境下 Python 会攒满一批才吐出一次，而任务
     脚本每行很短，于是一次全量要跑很多行才会出现第一批输出 —— 用户看到的就是
     「点了没反应，过很久突然冒出一大段」。现在改为实时逐行输出，面板才真的能
     当进度看。

  2. **超时判据用错了**：此前按「总时长 30 分钟」判定，而这个脚本的耗时**随账号数
     线性增长**（每个账号约 40 个动作、每个动作至少间隔 1 秒），账号多时光是下限
     就超过 30 分钟 —— 于是**正常的批量任务会被中途掐断**，做了一半还得重跑，比不
     设超时更糟。现在改为按「多久没有新输出」判定：还在产出就说明活着，静默超过
     5 分钟后才判卡死；另设 4 小时总上限兜底。

### 上游核对
- 核对上游最新提交，确认**无需适配**：对外接口、状态字段、配置项、日志文案都没有
  变化；改动都在上游内部（账号池选号、状态持久化、并发竞争修复等），属于它自身的
  缺陷修复 —— 更新你本地的上游容器即可受益，本管理端不用改。

## [1.0.44] - 2026-09-16

### 新增
- **成长任务一键执行（issue #19）**：成长中心那批加积分的任务此前只能上服务器手敲
  命令（上游自带的任务脚本被定性为「辅助工具」，上游调度器只自动跑签到、活跃地图、
  猫猫旅行、保活、开学季、夜猫这六类，其余没有任何自动入口；本面板此前也只看不跑）。
  现在「任务」页可直接发起。

  **按风险分成三档，界面措辞与确认流程都据此设计**（不是三个平级按钮）：

  - **预览** —— 只查询、不发任何写请求，随时可点；
  - **一键领奖** —— 只把**已完成任务**的奖励领回来，可重复执行、不伪造任何行为；
  - **做任务** —— 点亮 + 领奖。**会向腾讯发送活跃上报**（造画布、连续对话、批量
    使用专家等，这些行为是程序伪造的），有风控风险，因此必须二次确认，弹窗里写明
    风险并建议先看预览；上游自己也把这类批量写操作标注为「慎用」。

  其他安全边界：**定时只跑「领奖」**（点亮绝不进定时）；一次只跑一个；账号标识做
  字符白名单后作为独立参数传入（不存在命令注入面）；脚本或账号目录缺失时**提前
  拒绝**并说明该怎么办，而不是给一堆点了没反应的按钮；仅管理员可用；国际版视图下
  不显示（国际版没有这套体系）。

### 修复
- **Anthropic 协议：客户端「本轮只调一个工具」的限制被忽略**。客户端发
  `tool_choice: {"type": "auto", "disable_parallel_tool_use": true}` 表示这一轮只准
  调一个工具，但这个开关藏在 `tool_choice` 对象里、不在顶层，翻译层把它整个吃掉了 ——
  症状是「看起来一切正常」，客户端按串行编排，上游却可能一次回多个工具调用，编排
  逻辑拿到意料之外的结果。同类地，`type: none`（明确要求本轮别调工具）也被忽略，
  等于放开了客户端的限制。

  现在两者都正确传递给上游。注意**只在显式开启时才传递**：未给该字段时保持默认
  行为不变。

- **上游错误的「建议」信息在部分协议下丢失**：上游在错误响应里会附带一条可执行的
  建议（例如「没有可用账号，检查 /status 或稍后重试」）。原样转发的
  `/v1/chat/completions` 天然带着它，但两个协议翻译层（Anthropic Messages /
  Responses）重建错误体时只取了错误消息，建议被丢掉 —— 同一份上游错误，走不同协议
  的客户端看到的信息量不一样。

  现在三种协议一致：错误里带上建议，**没有建议时不编造**；流式响应中途出错时也会
  把建议并入报错文本（否则等于建议消失）。

- **安全页的审计详情在英文 / 韩文界面下仍显示中文**：改过哪些配置段、改密码/改角色
  等标记没进翻译表，于是原文透传。现在这些标记按界面语言显示（配置段名本身是键名，
  保持原样）。

### 上游核对
- 核对上游最新提交：**只有上面那条「错误建议」需要跟**。其余确认无需适配 —— 对外
  接口、状态字段、配置项、日志文案都没有变化，改动都在上游内部（选号算法、状态
  持久化、成本台账等）。

## [1.0.43] - 2026-09-16

### 适配上游
- **账号「连败降权」的三个新配置项**：上游新增了「连续失败达阈值就把账号临时移出
  队列」的机制，对应三个配置项（阈值、冷却时长、冷却上限）。设置页暂未提供输入框，
  但**手写在 `config.json` 里的值会原样留住**（保存其它字段不会把它丢掉），并且
  会做取值校验，填错会当场拒绝而不是写进一份让上游起不来的配置。

### 修复
- **首页说「在线」、账号列表说「未加载」——同一个账号两处说法互相矛盾**：首页的
  「账号健康快照」拿的是令牌有效期分档（讲的是「令牌还有多久过期」），于是只要
  令牌没过期就显示「在线」，账号有没有被禁用、冷却、是否真在上游池子里一概没看；
  账号列表走的却是另一套判定。

  现在两页共用同一套判定与同一套文案，顺序为：已停用 → 令牌过期 → 状态未知 →
  冷却中 → 一直失败 → 未加载 → 在线。同时修掉三处连带问题：首页「有效期内」卡片
  不再单凭令牌就写「全部正常」，而是优先显示有几个不可用；快照新增各档汇总
  （如「● 在线 1 · ● 未加载 1」）；令牌分档的标签由「在线」改为「有效」——它本来
  只描述令牌，叫「在线」容易被当成账号状态。

  另外，「读不到上游」与「上游明确没加载它」仍是两回事：前者显示「状态未知」，
  后者才是「未加载」。混为一谈会把正常账号误报成文件损坏（此前真实发生过）。

## [1.0.42] - 2026-09-16

### 新增
- **OpenAI Responses API 兼容（`/v1/responses`）**：Codex、DeepSeek Harness 的
  `openai-responses` 协议，以及任何用 OpenAI 官方 SDK `responses.create` 的客户端
  现在可以直接接本网关。此前只提供 Chat Completions，这类客户端要么用不了，
  要么被误导——Responses 与 Chat Completions 不是「参数改名」的差别：请求体是
  `input`（而不是 `messages`）、`instructions` 是顶层字段、工具是扁平形状；
  响应体是 `output` 数组；流式是一串 `response.output_text.delta` 事件。
  已实现双向翻译（含流式状态机、推理内容与并行工具调用），并覆盖
  `function_call` / `function_call_output` 的多轮工具回放。SDK 的 baseURL 带不带
  `/v1` 两种写法都支持（`/v1/responses` 与 `/responses` 都注册）。
  与 Anthropic 兼容层同思路：**只做协议翻译**，鉴权、版本隔离、IP 管控、
  配额、限流、日志与用量记账一律复用既有实现。

### 修复
- **「反复新建密钥也修不好」的报错：真实原因被客户端显示成「API 密钥无效」**。
  用 DeepSeek Harness 这类客户端时，密钥版本不匹配（例如在国内版界面建的密钥
  去调国际版模型）会被显示成 `本轮运行失败 API 密钥无效`，于是用户反复重建密钥——
  而在国内版界面里新建的每一把都还是国内版专用，永远好不了。根因有两层：
  - 网关把**所有**密钥约束拒绝都回成 403，而该客户端把 401/403 一律折叠成本地化的
    「API 密钥无效」（`status === 401 || 403 → code 'AUTH'`，界面再按 code 覆盖文案），
    真实报文被**整段丢弃**；
  - 版本不匹配本是「这次请求的模型选错了」这种配置问题，不该按认证失败上报。
  现在按「该怪谁」分流状态码：请求参数类（版本归属、模型白名单、缺 model、
  IP 白名单）→ `400`；凭据不可用（停用、过期）→ `403`（显示成「密钥无效」是贴切的）；
  配额用尽 → `429`。报文里保留可执行的下一步提示（如「模型名需带 `global:` 前缀」）。
  验证方式：按客户端的折叠规则复算一遍，确认用户看到的确实是真实原因。

- **上游调用失败时，管理端日志的「错误」列是空的**：网关流式分支里取上游错误体的
  写法是「缓冲超过 4000 字节才取一次」，而上游的错误体通常只有几百字节（实测
  111 字节），于是**错误被静默丢弃**——客户端那边看得到原文，管理端日志却什么都没
  记，用户来问「为什么失败」时查不到任何线索。与 Anthropic 层同口径改为「有数据
  就留一份」。**上游 `5755fe3` 恰好把错误响应改成了原样透传**（error.message 装
  上游 body 原文，有意让排查看得到原文），所以这一侧更不该丢。

- **国际版单账号并发上限的说明写错了**（仅注释与文档，行为未变）：该字段的 `0`
  在上游配置归一化时会被改成默认值 **2**——既不是「不限制」（那是
  `max_in_flight` 的语义），也不是「跟随 `max_in_flight`」。上游池子层的注释
  这么写，但归一化在它之前就把 0 换成 2，实际生效的是 2。界面文案本来是对的，
  这次把后端注释纠正到同一口径。

### 上游核对（截至 `2680f4c`，31 个提交）

逐个核对了自上次适配点（`3b87c14`）以来的 31 个上游提交，**协议与配置契约均无
破坏性变化，无需适配**：

- `Status` 结构体与 `/healthz` 输出**逐字节一致**（我们直连读取的两处形状没变）。
- `config.json` 只新增 `pool.max_in_flight_global` 一个键，我们已在 v1.0.40 登记。
- 新增的收益类型（新手礼包 / 活动补偿 / 补签卡）我们早已能识别，日志格式逐字对得上。
- 模型富字段（`name` / `description` / `credits` / `tags` / 推理能力）是上游
  `/v1/models` 的变化；我们的模型页走**直连腾讯**那条路，不受影响。
- 其余为上游内部修复（错误分类词表、选号权重、负缓存避让、退避基建）、
  自带 Python 脚本任务与文档，均不涉及我们的转发路径或直连路径。

以上改动会在下个版本一并发布。

## [1.0.41] - 2026-09-16

### 修复
- **上游暂时连不上时，账号页会把正常账号说成「文件损坏」**：上游容器重启或网络
  抖动的那一小会儿，面板读不到账号池，于是把每个账号都标成「未加载」并提示
  「请到上游保存设置重载配置 / 检查账号文件是否完整」——把「看不到上游」误报成
  「你的账号文件坏了」。账号页每 30 秒会自己刷新，所以这个误报会**反复出现又
  自行消失**，看起来像账号随机坏掉。

  现在这种情况显示 **「● 状态未知」**，并明确说明是读不到上游状态、稍后会自动
  重试，本地能确认的信息（令牌有效期等）仍然准确。只有当上游**确实返回了账号池
  但里面没有这个账号**时，才会显示「未加载」。

- **IP 白名单里多一个空格，那把密钥就会拒绝所有来源**：白名单支持
  `1.2.3.4` / `10.0.0.0/8` 两种写法，但从别处复制粘贴时很容易带上首尾空格。
  此前这种条目能保存成功，却匹配不上任何 IP——于是这把密钥**对所有来源都失败**，
  而报错只说「不在白名单内」，看不出是空格的问题。现在保存时会自动去掉首尾空格
  （已经存进去的旧数据也一并修正）。

- **统计页「统计可能没有正常写入」的误报**：如果某天只有被拒绝的调用（IP 被拦、
  密钥无效、限流），本来就不会产生用量数据，但此前会弹这条黄色警告。现在只有
  「**本该累计用量的调用**存在、但统计确实为 0」时才提示。

  > 同一处的另一个问题：这条提示的文案改成带版本号后，其余四种语言的译文失效
  > （非中文界面会显示中文句子），已一并修好。

- **任务记录里「连登抽奖」的积分显示为 0**：抽奖中奖的奖品名是中文（如 `10 积分`），
  而我们此前按英文格式（`50 credits`）解析，导致真实日志一律提取成 0。现在按
  奖品类型正确识别（积分奖计入收益，实物奖与未中奖算成功但无收益）。

- **任务记录里新增的奖励日志显示英文原文**：连登奖励、新手礼包、活动补偿、补签卡
  这几类（上游新增）此前没有中文说明，页面上是一行 `gift ok (+100 credit)` 这样
  的开发者文本。现已补上。

- **测试台（聊天试调）的调用被记成国内版**：在国际版测试台发起的调用，日志与用量
  里被归属到国内版，导致按版本筛选时看不到。现已正确归属。

- **日志页切换版本后停留在旧页码**：在「第 3 页」切到另一个版本，会去查那个版本的
  第 3 页——可能一页都没有，于是看到空白表格和「第 3 页 / 共 1 页」的矛盾页码。
  现在切版本会回到第 1 页。

### 安全
- **入站访问日志可以被无限写入**：这张表记录被拒绝的访问（IP 被拦、密钥无效等），
  写入发生在校验之前——也就是说**不需要任何凭据**就能写。此前它既没有长度限制也
  没有清理机制，带上超长的 User-Agent 反复请求就能把数据库撑大直到磁盘写满，
  进而拖垮管理端与上游。现在单条记录会截断、总量超过上限后自动丢弃最旧的
  （保留 2 万条，足够回溯近期访问）。

- **「连通性测试」的地址校验可被绕过**：设置页里测试 Redis 地址时，服务端会代为
  发起请求。此前的拦截能被几种写法绕过（`localhost`、`user@127.0.0.1`、
  `127.0.0.1.nip.io` 这类通配域名），从而探测内网或云厂商的元数据端点（后者常能
  拿到实例临时凭据）。现已按**解析后的真实地址**判断，四种绕过写法全部拦截。

- **日志里的换行字符未过滤**：带换行的 User-Agent 或模型名能在日志页**伪造出额外
  的日志行**，干扰事后排查。（审计日志一直有过滤，网关这两张表此前漏了。）

- **超出常规的请求会占用过多资源**：超长的模型名会被重复存进数据库三处，单次请求
  就能放大数 MB；管理端的请求体此前也没有大小上限（登录接口无需凭据即可访问）。
  现已分别加限制。

- **上游配置的敏感字段改为白名单下发**：此前是「列出已知密钥并屏蔽」，因此上游将来
  新增的任何凭据字段都会被**原样下发**给所有登录用户（包括只读账号）。现改为只下发
  界面需要的配置段。

---

## [1.0.40] - 2026-09-16

### 新增
- **设置页可单独设置国际版的账号并发上限**：上游新增了这个分档（国际版风控更严，
  官方默认压到 2，避免并发过高被判为异常流量），此前我们面板没有对应入口 ——
  用户只能去手改 `config.json`。现在在「账号池」里就能调（不填则跟随通用的那个）。

  > 顺带把面板与上游的配置项做了一次**全量比对**：上游 48 个键里有 37 个已可视化，
  > 其余是超时三项与 `device_token`（有意不暴露——改错会让出站形态不像官方客户端），
  > 以及 `upstash` 的两项（有专门界面）。也就是说本次补上之后没有遗漏。

### 修复
- **任务记录的收益在解析器里「逐个动作列举」，每次上游加新动作就漏一次**：本次
  上游新增了新手礼包与活动补偿（`gift ok (+100 credit)`），收益显示为 0 —— 数字是
  错的却不报错，属最难发现的一类。已改为**通用规则**（`<动作> ok (+N credit)` 即可
  提取），上游以后再加动作不用跟。

  > 这已是第三次（`adopt` → 连登 `redeem` → 本次 `gift`），所以这次改的是根因。

---

## [1.0.39] - 2026-09-16

### 修复

- **账号页显示「在线」，调用却报「没有健康账号可用」**：这是账号状态判定与上游
  实际选号口径不一致导致的——**面板读的是账号文件，上游读的是账号池**，两者不总
  一致。

  最典型的一种：账号文件的 `accessToken` 是空的（重新登录中断、文件被手工改过、
  写入被截断）。上游加载时会**静默跳过**这种文件，它因此不在池里、永远选不中；
  而面板照样把它列出来，且因为上游状态里没有它，各种标记字段都是空的，就落到了
  「● 在线」。**池里若全是这种账号，面板全绿而每次调用都失败**。

  现在这类账号会显示 **「● 未加载」**，悬停说明原因（例如「缺少 accessToken」）
  并提示怎么做：到上游保存设置重载配置；若仍如此，重新登录或删掉重加。

  > 顺带说明：上游连不上时也会显示「未加载」——这是对的，那时确实一个账号都
  > 选不中（面板上方会同时提示上游不可用）。

- **任务记录里「领到积分」显示为 0**：上游新增了新手礼包与活动补偿领取
  （`gift ok (+100 credit)` 这类日志），而我们的收益解析是**逐个动作列举**的，
  没认出的新动作就按 0 记 —— 数字是错的，但页面上不会有任何报错，属于最难发现
  的一类问题（此前已经踩过两次）。

  现改为**通用规则**：`<动作> ok (+N credit)` 即可提取，不再需要为新动作补代码。
  同时收紧条件（要求括号里只有 credit 且紧跟右括号），因此不会误吞连登奖励那种
  `(+100 credit, +5 energy, +1 chances)` 的多项括号。

---

## [1.0.38] - 2026-09-16

### 新增

- **界面多语言（简体中文 / 繁體中文 / English / 日本語 / 한국어）**：管理端原本
  只有简体中文，非中文用户无法使用。现在右上角可切换（登录页也有，未登录也能换），
  默认跟随浏览器语言，选择记在 `localStorage`。

  不做语言路由前缀而是客户端切换：前端是 `next build` 静态导出、由 FastAPI 托管，
  加 `/en/...` 要改所有跳转与后端静态路由；而语言偏好本就是单个使用者的本地设置。

  - 两种取值方式分工明确：界面文案走**键名**（`t('accounts.title')`）；设置页那上百条
    与上游 `config.json` 一一对应的配置项说明走**短语表**（`tp('中文原文')`，
    gettext 风格），省掉一层「键名 ↔ 文案」映射，新加上游字段时照抄原文即可
  - **后端返回的文案也跟随语言**：服务端返回的错误文案会过一遍短语表，
    因此服务端报错、签到结果、积分流水等固定文案同样会翻译；未收录的保持中文原文，
    不会显示成键名或空白
  - 刻意不翻的内容（CHANGELOG 正文、后端枚举值、开发者可见的异常）与取舍都写在
    [docs/i18n.md](docs/i18n.md)
  - 新增结构守卫 `server/tests/test_web_i18n.py`（键集一致、占位符一致、无空译文、
    英文版不得残留汉字、短语表键集一致）与两个开发工具：`dev/i18n-scan.mjs`
    （扫描未提取的硬编码文案）、`dev/i18n-verify.mjs`（真浏览器逐语言逐页取可见
    文本，报告残留中文）

- **Windows 本地启动脚本**：Windows 上没有 systemd，无法套用 `deploy/install.sh`
  那一套。新增 `start.cmd` / `start.ps1`（前台启动）与 `service-tools.ps1`
  （`start` / `stop` / `status` / `restart`，后台独立进程，日志写 `data/`）、`stop.ps1`。

  其中 `PYTHONUTF8=1` 是 Windows 上的必要设置：中文 Windows 默认 GBK，而
  `docker logs` 与腾讯接口返回的都是 UTF-8，不开会让「读上游日志」的线程抛
  `UnicodeDecodeError`、任务记录页取不到数据。**只在启动脚本里设环境变量，
  未改动项目源码。**

### 修复

- **Docker 构建在国内网络下直接失败，导致容器起不来、页面 ERR_CONNECTION_REFUSED**：
  官方软件源不可达——`deb.debian.org` 走 Fastly CDN，实测约 367 kB/s 且频繁返回 502，
  `apt-get update` 在第 2 步就中断（拉 PyPI 也有同类 `SSL: UNEXPECTED_EOF`）。
  这种「端口拒绝连接」很容易被误判成服务问题，实际是**镜像根本没建出来**。

  - 新增 `DEBIAN_MIRROR` / `PIP_INDEX_URL` 两个构建参数，可显式指定国内镜像源
  - **并内置一次自动降级**：官方源失败时自动改用阿里云（apt）/ 清华（pip）重试，
    不配任何参数也能构建成功。降级分支用「故意指向不存在的镜像源」实测过
  - 调小 apt 的重试与超时，减少官方源不通时的无谓等待
  - `deploy/README.md` 增补排查步骤（含「打开 7863 显示 404 是正常的」这一高频困惑：
    7863 是纯 API 的上游网关，本就没有网页界面）

- **按钮里的图标又大又虚**：按钮内图标本应是 16px，但 `[&_svg:not([class*='size-'])]:size-4`
  经 Tailwind 编译出来是 `svg:not([class*="'size-'"])`——属性值里多了两个引号，
  **永远匹配不到**，图标一直按 lucide 默认的 24px 渲染（按钮才 32px 高）。
  现已改为在 `globals.css` 里统一处理：默认 16px、描边与渲染保持 lucide 默认
  （与底部工具栏图标一致）。选择器同时覆盖 trigger——`<ConfirmDialog trigger={<Button/>}>`
  这类组合里 Radix 的 `asChild` 会把 `data-slot="button"` 覆写成 `alert-dialog-trigger`，
  只匹配 button 会漏掉「删除账号 / 强制重启 / 清空日志」等危险操作按钮。

  > 记一次该走回头路的弯路：起初为了让自造的「实心黑 / 中间灰像素比」指标好看，
  > 加过 `stroke-width: 3` 与 `shape-rendering: crispEdges`，指标从 1.2 升到 4.6，
  > 但**观感反而更糊**——crispEdges 关掉弧形描边的抗锯齿，`hand-coins` 这类小尺寸
  > 曲线图标直接糊成色块。指标被骗的原因是它奖励「灰像素少」，而抗锯齿消失恰好让
  > 灰像素归零。结论：小尺寸曲线图标必须保留抗锯齿；判断清晰度要看真机截图，
  > 不要只看像素统计（`dev/icon-ab.mjs`、`dev/icon-probe.mjs`）。

- **Anthropic 兼容层：客户端同时带多份凭据时必然 401**：Claude Code 有时会同时发
  `x-api-key`（值属于**另一个服务**，实测是 67 字符的 `sk-…`）与
  `Authorization: Bearer`（值才是本网关的 `wbk_…`）。原实现只取第一个，于是
  「明明配对了却一直 401」，而客户端侧完全看不出问题。现改为收集**全部**候选并
  逐个尝试；同时兼容 `x-anthropic-api-key`，以及不带 `Bearer ` 前缀的裸 token
  （部分转发工具如此发送）。

- **Anthropic 兼容层：上游流异常时的三处静默失败**：

  - 流式解析缓冲**没有上限**：上游若持续吐出不含换行的数据，缓冲会一路长到吃光
    内存。现设 1 MiB 上限并中止转发（正常单行 SSE 即使带大段 tool_use 参数也只
    几十 KB）。
  - 上游**中途**回 error 帧（`{"error":{…}}`、没有 `choices`）时整帧被丢弃，
    客户端只收到 message_start 加一个空回答。现转为 error 事件并中止。
  - `usage` 字段畸形（字符串 / null / 嵌套对象）会让 `int(…)` 抛错，而异常发生在
    **流式生成器内部**——直接掐断流，客户端连 `message_stop` 都收不到。现统一走
    容错转换，转不动按 0 计。

- **Anthropic 兼容层：`error` 事件在「状态码 200 但中途出错」时的顺序**：这种
  情况（缓冲超限、上游中途报错）同样要显式告知客户端，否则它拿到的是一个
  `stop_reason=end_turn` 的**完整但空/残缺**的回答——不重试、不报错；截断点落在
  `input_json_delta` 中间时，客户端还会拿半截 JSON 去执行工具调用。现与「上游直接
  报错」分支合并为同一段收尾逻辑，错误一律发在 `message_stop` **之前**（理由同
  上条顺序说明）。

- **Anthropic 兼容层：请求体超限被误报成格式错误**：`/v1/messages` 收到超大 body
  时一律回 400「请求体不是合法 JSON 对象」，客户端只会原样重试；现保留网关注入的
  413 语义，并给出可操作的建议（压缩上下文，或调大 `server.max_body_mb`）。

- **切到国际版后统计页误报「统计可能没有正常累计」**：页面提示「今天已有 605 次
  调用记录，但用量统计为 0」，而点「修复统计」又回答「统计与请求日志一致」——
  两条结论互相矛盾，让人无法判断到底坏没坏。

  原因是健康检查的口径不一致：日志条数按**全部版本**统计、用量按**当前版本**过滤，
  所以「只有国内版流量、没有国际版流量」时，切到国际版必然触发误报。现已统一按
  同一版本口径统计（历史记录归国内版），提示里也会写明是哪个版本。

- **日志页切换国内版 / 国际版后要等 60 秒才刷新**（#15）：分页与天数会立即重新
  查询，版本切换漏了 —— 点了之后要等下一次自动刷新（60 秒），看起来像点了没反应。
  现已与分页/天数同属「立即重查」。

- **账号被风控后一直显示「正常」，实际每次都失败**（#14）：上游对**未命中它那几条
  规则**的 403（例如被 WAF 拦下）只「换号不罚」——不冷却、不禁用，所以这种账号在
  面板上状态健康、继续参与选号，但每次请求都失败、也不会自愈。

  选号逻辑在上游（我们无法干预），但可以把它标出来：账号页现在会显示
  **「● 一直失败」**（判据：有累计错误、且从未成功过），悬停可看尝试次数与说明。
  被风控的号建议直接删掉或重新登录，留着只会一直占选号名额。

- **密钥列表的表头多出一排错位列**（合并多语言时的疏漏）：表头被重复了一遍，
  页面上多出「有效期 / IP·模型 / 已用 Token / 最近使用 / 操作」一行，与下方数据列
  对不齐。现已恢复为 9 列。

### 安全

- **Anthropic 兼容层：`/v1/messages/count_tokens` 的鉴权不完整**：该端点原先只确认
  「密钥能解析」，**没有走完整校验**——停用、过期、配额超限、模型/版本白名单、
  IP 管控、限流**全部绕过**。管理员停用一把泄露的密钥后，`/v1/messages` 会正确
  返回 403，`count_tokens` 却仍是 200，等于**吊销不彻底**。现已与 `/v1/messages`
  共用同一套鉴权。

  > 根因是「鉴权逻辑被实现了两份」：任何一处安全项改动只落在一份上，两边就开始
  > 漂移——这次漂移掉的正是整段校验。现已抽成单一函数，不再有第二份。

- **Anthropic 兼容层：`count_tokens` 无请求体上限**：它直接 `await request.json()`
  读体，没有体积检查——持密钥者发一个超大 body 就能把网关内存打满（同样的请求打
  到 `/v1/messages` 会被 413 挡下，因为那条路走的是网关的读取实现）。现已改用
  同一实现，并保留 413 / 400 的区分。

---

---
## [1.0.37] - 2026-09-16

### 新增
- **兼容 Anthropic Messages API（`/v1/messages`）**：Claude Code / Cursor / Cline /
  Zed 等**只认 Anthropic 协议**的客户端现在可以直接接入本网关。

  此前它们用不了——不是配置问题，而是协议不同：`system` 是顶层字段（不在 messages
  里）、`max_tokens` **必填**、`content` 是 block 数组、流式是带 `event:` 行的事件流。
  光靠改 `ANTHROPIC_BASE_URL` 指过来是不通的。

  - 令牌两种传法都认：`x-api-key`（Anthropic SDK）与 `Authorization: Bearer`（Claude Code）
  - **鉴权、IP 管控、配额、限流、日志与用量记账全部复用网关既有实现**，不另起一套
  - 流式按规范逐事件转换：`message_start` → `content_block_start` →若干
    `content_block_delta` → `content_block_stop` → `message_delta` → `message_stop`
  - 工具调用双向映射：OpenAI 的**分片** `arguments` 字符串 ↔ Anthropic 的
    `input_json_delta` 事件；非流式的 `input` 由字符串还原为对象
  - 工具结果的消息形态差异也一并处理：Anthropic 把它放在 user 消息的 block 里，
    OpenAI 要求是独立的 `role: tool` 消息（转换时会拆开并前置）
  - `/v1/models` 按 `anthropic-version` 请求头分流为 Anthropic 形状
    （两边路径同名但结构不同，只能按头分流；各注册一个路由会有一个永远收不到请求）
  - 另提供 `/v1/messages/count_tokens`（按字符数粗估——拿不到上游分词器，
    宁可高估留余量，也不低估导致真实请求超限）

  > 与「转发热路径原样透传」不冲突：本层只改变**回给客户端的表达形式**，
  > 不改动转发给上游的内容。`test_gateway_passthrough` 已相应标注这一例外。

### 修复
- **新建密钥提示「Internal Server Error」，但密钥其实已经建好了**：这个报错很
  容易让人误判——以为没建成，于是再点一次，结果建出**多把重复密钥**。

  真正的原因不在创建本身：创建请求是成功的（密钥确实入库了），报错来自紧接着
  的**列表刷新**。密钥的「IP 白名单」和「模型白名单」在库里是一段 JSON 文本，
  只要**任意一把**密钥的这两段存了不合法的内容（历史版本写入过、手工改过库、
  写入被截断），整个密钥列表接口就会报错——界面上表现为一把密钥都看不到，
  而创建那个动作看起来「失败」了。

  现已改成：某把密钥的白名单读不出来时，**只把那一项当作空**（即该项不限制），
  不再让一条坏数据拖垮整个列表。其余字段、其他密钥都不受影响。

  > 一点提醒：如果你之前遇到过这个报错，去密钥页看一遍——可能有几把同名密钥
  > 是重复点击建出来的，确认后删掉多余的即可。**已经建好的密钥它的白名单可能
  > 需要重新设置一次**（就是那条读不出来的）。

- **连点「创建」会建出多把同名密钥**：按钮虽然有「提交中」状态，但那个状态要等
  界面重绘才生效，快速连点或按回车时，第二次提交可能赶在它生效之前发出去
  （实测连点三次进三把）。现改为在提交的**同一瞬间**就上锁，与界面重绘无关；
  失败时释放，所以报错后仍可重试。

  > 顺带修了一处相关的小问题：列表刷新失败不再被当成「创建失败」提示——密钥
  > 那时已经建好了，会改为提示「已创建，但列表刷新失败」。

- **IP 白名单里写错一个条目，那把密钥会彻底用不了**：白名单支持 `1.2.3.4` 和
  `10.0.0.0/8` 两种写法，但如果写了一行非法内容（比如漏了斜杠、IP 段写错），
  它匹配不上任何 IP——于是这把密钥**对任何来源都被拒绝**。而此时的报错是

  ```
  来源 IP x.x.x.x 不在密钥白名单内
  ```

  看不出是白名单本身写错了，只会以为密钥坏了或者 IP 变了，很难往那个方向查。
  现在这类内容在**创建/编辑时就会被拒绝**，并明确告诉你哪一条写错了、该怎么写。

- **密钥名称只填空格也能建出来**：列表里会出现一行「没有名字」的密钥，管理员
  认不出它是干什么的，也看不出是自己误操作建的。现在会被拒绝。

- **对已被删除的密钥点「重置用量」会提示成功，但实际什么都没发生**：现改为
  如实提示该密钥不存在。

- **Anthropic 兼容层：上游报错时客户端收不到错误**（合并前评审发现，已在本版修掉）：
  流式请求遇到上游报错（模型不存在、额度不足等）时，客户端收到的是**「成功但内容
  为空」**——一套正常的 message_start → message_delta → message_stop，没有任何
  错误信息。对 Claude Code 这类客户端，表现为模型静默返回空回复，比直接报错更难
  排查（用户会以为是自己 prompt 的问题）。

  两处成因，都在同一段收尾逻辑里：

  - **错误体收集的阈值写反了**（`len(pending) > 4000`）。真实的上游错误体只有
    一二百字节，永远不满足这个条件，于是错误文本恒为空。
  - **事件顺序错了**：错误事件被发在 `message_stop` **之后**。多数 SDK 把
    `message_stop` 当作流的终止信号，读到就不再读后续事件——那个错误永远不会
    被看到。现已改为先发错误、再补收尾事件（部分客户端需要 message_stop 来结束
    等待，缺了会挂住）。

  非流式路径不受影响（它本来就是对的），所以这个缺陷只在流式下出现。

---
## [1.0.36] - 2026-09-16

### 修复
- **从旧版本升级上来的部署，用量统计会彻底停止累计（且毫无提示）**：这是最需要
  你留意的一条——**只有「建库早于 v1.0.33、之后升级上来」的部署会踩到，全新安装
  不受影响**，所以你如果装的是新版就不会遇到。

  症状是统计数字**冻结在升级前**：仪表盘、统计页、按模型/密钥的明细都不再更新，
  但页面照常刷新、接口也正常返回 200，看不出任何异常。同时「修复统计」按钮点下去
  报错。

  原因是升级时只给旧库加了个字段，而底层的写入语句早已改成用「四个字段」去匹配
  唯一记录——旧库还是三个，两者对不上，于是**每一次统计写入都失败**。这个失败被
  设计成不影响你的正常调用（统计是旁路），所以只留在服务端日志里：

  ```
  累计用量失败（不影响请求）: ON CONFLICT clause does not match any PRIMARY KEY...
  ```

  现在升级时会自动把这个表按正确结构重建，**历史数据完整保留**，且可重复启动
  （不会重复迁移）。**你的原始调用日志一直
  是完整的**，所以即使之前统计停了，更新后点一次「修复统计」就能把缺的补回来。

  > 另外加了可见性提示：统计页若检测到「今天有调用记录、但统计为 0」这种不该
  > 出现的组合，会直接显示一条黄色警告并指向「修复统计」按钮。这个缺陷拖了几个
  > 版本才被发现，就是因为它是静默的——写入失败不影响转发，界面看起来一切正常。

- **国际版模型列表还是缺 DeepSeek（这次找到真正的原因了）**：此前只修了一半——
  模型目录在腾讯那边是**两级取数**，官方客户端会同时问企业接口和一个我们没问过
  的接口，部分模型只在后者下发。只问企业接口就会丢掉它们，实测国际版少了
  `deepseek-v4.1-flash`、`gpt-6-astra`、`hy4-preview-f`、`kimi-k2.8-preview`
  四个（`hy4` 系列也在其中）。现已两路一起取、按模型 id 合并去重，
  任一路失败仍能用另一路的结果（不会因为一边抽风整页取不到模型）。

- **连登奖励的积分没算进收益、抽奖中奖显示成失败**：上游新增了连登奖励领取与
  连登抽奖（连续登录到 7/14/28 天可领积分）。它们的日志挂在与「活跃上报」相同的
  类型下，所以任务记录页能收到，但结果文案是新格式：

  - 领奖的积分**显示为 0**（拿到 +100 却显示没赚）——现在按 credit 段正确计入
    （energy / 抽奖次数不是积分，不混进来）；
  - 抽奖成功**被判成失败**（红色）——它的文案里没有「ok」字样，落到了兜底分支。
    中奖显示成报错比不显示更糟；现在按成功处理，积分奖也计入收益。

- **容器镜像不支持 arm64，ARM 机器拉下来用不了**：发版流程用 `docker build` 构建，
  只产出 runner 自身的架构（amd64），所以 Apple Silicon、ARM 云主机（Oracle、
  华为鲲鹏等）拉镜像时会直接报 `no matching manifest for linux/arm64`。
  现已改为用 buildx 同时构建 `linux/amd64` 与 `linux/arm64` 并合并成一个
  manifest list，`docker pull` 按机器架构自动选择。

  > 同一个问题的第二处：`Dockerfile` 里安装 docker CLI 时把静态包的路径写死成
  > `x86_64`。而 Docker 官方静态包的目录名与 Docker 自己的架构名并不一致
  > （amd64→`x86_64`、arm64→`aarch64`），于是即便镜像能拉下来，容器内的
  > `docker` 命令在 ARM 上也是一个跑不起来的 x86_64 二进制——要等到调用它
  > （重载上游、读上游日志）时才报「格式错误」。现在按目标架构选包，
  > 并在无法识别的架构上**构建期直接失败**，而不是产出一个坏镜像。

  > 直接 `docker compose up --build`（不走 CI）的 ARM 用户同样受益：没有 buildx
  > 时按宿主机架构回退取包，不再需要手工改 Dockerfile。

---

## [1.0.35] - 2026-09-15

### 新增
- **API 密钥可以限定版本了——国内版密钥只能调国内版，国际版密钥只能调国际版**：
  新建密钥时选「仅国内版 / 仅国际版 / 不限制」，默认跟随你当前所在的版本。

  此前密钥只有一份**模型白名单**，要手工往里填 `global:` 前缀的模型名才能起到
  隔离作用——不填就等于两版都能调，而界面上并不会提示这一点。现在版本归属是
  密钥自己的属性，由网关强制执行：

  - 用国内版密钥调国际版模型（或反过来）→ 直接拒绝，并说明该改哪里；
  - **调 `/v1/models` 时也按版本裁剪**——限定国际版的密钥只列出国际版模型，
    不会挑出一个注定失败的；
  - 版本判定与实际路由同源（都看模型名的 `global:` 前缀），因此和真实流量一致；
  - **不带 model 的请求会被拒绝**：那会走上游默认模型（国内版），否则限定国际版
    的密钥只要不发 model 就能打到国内账号池，隔离等于没有。

  > **老密钥不受影响**：本功能上线前创建的密钥保持「不限制」，不会因为升级就被
  > 限死（那会让线上正在跑的调用突然报 403）。它们在密钥列表里标为「未限定」，
  > 编辑一次即可选定版本。

### 修复
- **仪表盘的数字把两个版本混在一起算了**：首页「今日 Token / 请求数」与「近 14 天
  调用趋势」取的是**全局**数据，切到国际版也照样显示国内版加国际版的总和——而
  用量统计页早已按版本分开。同一份数据在两个页面对不上，容易让人以为统计坏了。
  现已统一：仪表盘跟随右上角的版本切换，并注明当前显示的是哪个版本。

- **国际版模型列表漏掉了国际版实际可用的模型**（含 `deepseek-v4.1-flash`）：
  模型清单取自腾讯的同一接口，但**国内版与国际版的取法并不相同**——国内版按
  接口里的 `cli` 清单过滤，国际版则是全量。此前把国内版的口径套到了国际版上，
  后果是国际版能用的模型只要不在那份 `cli` 清单里，就从模型中心消失，看起来
  像是「国际版没有这个模型」。现已按两边各自的口径分别取数。

  > 同一个错误也影响另一处：国内版还会剔除非对话模型（嵌入、补全、代码专用、
   > 输出上限过小、图片生成——挑中它们聊天必然报错），这套规则同样源自国内版，
   > 此前一并套在了国际版上。现在只对国内版生效。

- **国际版模型列表在接口返回「简表」时整页空白**：该接口有两种返回形态，一种是
  带完整信息的对象列表，另一种是只有模型名的字符串列表。此前只认前者，遇到后者
  会当作失败处理（页面上表现为取不到模型、并留下一条失败记录）。现已兼容。

### 改进
- **设置页「可用模型」的来源说明不再提「回退到内置静态表」**：上游已改为纯动态
  取数（取不到就是空列表，不再回退到写死在程序里的固定名单）。界面文案已同步；
  旧版上游的那条提示仍在，只是注明它只可能出现在较旧的上游版本上。

---

## [1.0.34] - 2026-09-15

### 新增
- **模型中心显示积分倍率，并可按倍率排序**：模型列表新增**积分倍率**（如 `x0.05`）、
  模型描述、厂商、以及「仅推理 / 默认模型」等标记。同一请求在不同模型上的扣费倍率
  不同，倍率越低越省积分——点「积分倍率 ↑」即可从低到高排，挑最省的模型。
  > 这些字段来自腾讯模型接口。上游这次也补齐了它的 `/v1/models`，两边口径一致，
  > 我们直连取的字段更全（含中文描述）。

### 新增
- **英文版说明文档**：新增 [README.en.md](README.en.md)，顶部可一键切换中英文。
  内容与中文版逐节对应（13 张截图、全部示例代码一致），并已接入发布链路
  —— 发布包、一键更新、安装脚本都会带上它，不会出现「更新完只有中文是新的」。

### 修复
- **容器部署下「更新上游」不可用（判断修正）**：上一版把「容器里不能更新上游」
  当成了不可协商的安全取舍，理由是「挂载 docker 套接字等于把宿主权限交给容器」。
  **这个理由不成立**：宿主部署时本服务本来就是以 root 运行的，而 root 进程本来
  就能挂载宿主根目录——两者权限等价。所以挂载套接字只是让容器版与宿主版能力
  对齐，并没有引入新的风险等级。

  现在容器版与宿主版**能力一致**：能更新上游、能自动重载配置、能读上游日志。
  对应的支持是——镜像内装 docker 命令行、compose 默认挂载 docker 套接字与上游
  仓库目录。若你确实要最小权限，把套接字那行注释掉即可：依赖它的功能会**自动
  降级为「请到宿主机操作」**并在界面提示（不会静默失败）。

### 修复
- **发布包漏打包 Docker 部署文件**：v1.0.33 的压缩包里缺少 `Dockerfile` 与
  `docker-compose.yml`（打包步骤只复制了程序与文档），导致下载发布包后无法
  直接用容器部署，得回仓库另取这两个文件。已补入打包列表。
  > 只影响「下载压缩包做容器部署」这一种用法：容器镜像本身正常（GHCR 可直接
  > `docker pull`），一键更新也不受影响（它只替换程序代码）。

---

## [1.0.33] - 2026-09-15

### 新增
- **支持 Docker 部署**：仓库现在自带 `Dockerfile` 与 `docker-compose.yml`，
  适合已经用 Docker 跑上游的用户；发版时会自动构建镜像推到 GHCR，可直接
  `docker pull`。容器里有三处与宿主机安装不同的**边界**（界面上会如实提示，
  不让人点了才发现）：

  - **不能在界面更新上游**：重建上游容器需要 docker 命令，而挂载 docker 套接字
    会把宿主权限交给容器内进程——风险远大于这点便利，因此刻意不做。请到宿主机
    执行 `docker compose up -d --build`。
  - **更新管理端会重启整个容器**：容器无法重启自己，更新流程是「替换代码 →
    结束容器 → 由 compose 的 `restart` 策略用新代码拉起」，所以 compose 里
    必须保留 `restart: unless-stopped`。
  - **端口默认只绑定本机**：管理端持有全部账号凭据，应藏在反向代理之后。
  - **改完设置需要到宿主机重启一次上游**：上游只在启动时读配置文件，改完必须
    重启它才生效。宿主部署时管理端能直接重启上游容器，但容器里没有 docker 命令
    （刻意不挂 docker 套接字），所以这一步要你手动做——保存设置后界面会提示。

### 修复
- **模型中心不显示推理档位（上游已支持多档强度的模型显示为「不支持」）**：例如
  `deepseek-v4.1-flash`，上游已支持 low/high/max 三档，页面上却是空的。原因是
  推理档位此前只依赖接口返回，而接口对部分模型不给这个字段。现已按上游的做法
  补齐：接口给了就用手上的，没给则查一份产品级档位表（**按国内/国际分开**——
  同一模型在两个版本的档位可能不同，例如该模型国内版三档、国际版仅 high）。

- **仪表盘「反代上游」面板的健康账号/冷却中/已禁用一直显示 0**：它读的是账号
  明细里的 healthy 字段，而**上游的账号明细里根本没有这个字段**——那是 `/status`
  响应**汇总层**才有的（`{"healthy": N, "cooling": N, ...}`），所以布尔转换恒为
  假，三项永远是 0。上游其实早就按版本分好了组（`realm_totals.cn` / `.global`），
  现已直接采用：数字与上游状态机口径完全一致，切换版本时也各算各的。

- **用量统计与请求日志切到国际版后仍显示国内的数据**：顶部切版本此前只影响了
  一部分页面，统计页还明确写着「含两种版本，不按版本过滤」——两个版本走的是
  不同账号池，混在一起看没有意义。现已按版本切分：
  - 「用量统计」的今日/本周请求、Token、实付、按模型、按密钥、趋势图都随版本变化
  - 「请求日志」列表也随版本过滤
  - 数据层补了版本归属（按模型名的 `global:` 前缀判定，与上游的路由协议一致）；
    升级时自动加列，历史记录归入国内版（它们本来就是单一版本的调用）

- **密钥的版本隔离现在有明确入口**：密钥本身按设计**不绑版本**——同一把密钥
  调哪个版本由**请求的模型名**决定（上游的路由协议如此）。要限定某把密钥只能
  用国际版，就在它的「模型白名单」里只列 `global:` 开头的模型；留空则两版都
  可用。密钥编辑弹窗已直接写明这条，不用再猜。

---

## [1.0.32] - 2026-09-15

### 改进
- **冷却中区分「被限流」与「该账号没有这个模型」**：账号冷却时，鼠标悬停会
  列出涉及的模型。但这两类原因的含义完全不同——**被限流**等一会儿会自动恢复；
  **该账号没有这个模型**则等多久都不会好（应该换个模型）。此前混在一起统称
  「被限流的模型」，会让人对着一个永远不会恢复的模型干等。

### 修复
- **账号的设备标识头补齐（继续贴近官方客户端形态）**：官方客户端会为每个账号
  带一个固定的「设备标识」，上游据此识别；缺失或漂移都可能被判定为异常流量。
  账号登录、签到、查积分、地区注册、试用包领取、连通性测试这些操作现已补上
  对应标识（按账号稳定派生，跨重启不变）。

- **「关于」里显示的版本号一直是 1.0.0（不会跟着更新变）**：它读的是前端
  包里一个手写的版本字段，而那个字段从建项目起就没再维护过。后果是**你刚
  更新完，看到的还是 1.0.0**，容易误以为更新没生效。现在改为显示后端实际
  运行的版本号，更新后即随之变化。
  - 同一行的「Build At」也是写死的日期（一直是 2026-09-11），现已改为构建时
    自动记录，反映的是这份前端产物真正打包的时间

- **更新时的 `deploy/` 差异提示现在会说明「要不要紧」**：`deploy/` 里放着
  更新包的验签逻辑，属于信任锚，因此**不随包自动替换**——这本身是设计。
  但此前的提示不论差异是什么都只说「已跳过同步 + 文件名」，于是「新增了一个
  无害的检查脚本」和「验签逻辑被改动」看起来一模一样，没法判断该紧张还是
  该忽略。现在会分开标注：

  - **仅新增** → 明确写「不影响本次更新，可以不处理」
  - **修改普通脚本/配置** → 「看过差异后按需覆盖即可」
  - **修改验签相关文件** → 显式警告，要求人工确认后才覆盖（这是供应链防护
    的最后一关）

---

## [1.0.31] - 2026-09-15

### 安全
- **登录接口遇到异常请求体会报错**：如果请求体是合法 JSON 但不是对象
  （`null`、`[]`、纯字符串、数字），服务端会返回 500（内部错误）。响应本身
  不泄露任何信息，但属于出错而非正常拒绝。现已改为返回「请求格式错误」。

- **修复「Upstash 连通性测试」可被用来探测内网与云元数据（SSRF）**：这个功能
  是把填写的地址交给服务端去请求、再把响应回显，但地址只做了格式归一化、
  没判安全性，因此可以填内网地址或云元数据端点（如 `169.254.169.254`、
  `metadata.tencentyun.com`，后者常能取到服务器临时凭证）让服务端去打。
  现在会拒绝回环、内网、链路本地与已知元数据域名；正常的 Upstash 地址
  （含自建/第三方兼容服务）不受影响。

- **收紧数据库文件权限**：请求日志、审计日志与 API 密钥哈希都存在本地数据库
  里，此前文件权限过宽（同主机的其他用户可读）。现在与用户文件一致，收紧为
  仅属主可读写，含 WAL 模式的两个伴生文件。

### 改进
- **账号「冷却中」现在显示还要等多久**：以前只写「冷却中」，看不出是等几秒还是
  等几小时，只能反复刷新碰运气。现在直接标出剩余时间（如「冷却中 · 12 分钟」）。

- **冷却中会列出被限流的模型**：多个模型同时被限流时，各自的恢复时间并不相同。
  鼠标悬停可以看到具体是哪些模型被限。

---

## [1.0.30] - 2026-09-15

### 修复
- **上游更新完成后，面板仍一直提示「有更新」**：更新检查的结果会缓存 6 小时，
  但更新上游后没有清掉这份缓存，界面就会拿更新前的记录继续比，于是明明已经
  是最新版，却还是一直显示有更新——最长要等 6 小时才自己恢复。现在更新上游
  后立即重新判断。

- **模型中心会列出「选不了」的模型**：嵌入、补全、代码专用、图片生成这几类
  模型（以及输出上限过小的），选中后会直接报错。现在不再列出。

### 改进
- **模型中心显示「默认推理档位」与「多模态」标记**：
  - 推理模型旁标注「默认 xxx」，不用再猜不指定档位时会走哪一档
  - 支持图片输入的模型打「多模态」标，并新增「多模态」筛选按钮

---

## [1.0.28] - 2026-09-14

### 修复
- **账号的「连通性测试」报错看不懂**：报错信息会显示成
  `上游返回 code=11-128 first message is not system prompt` 这样一串英文，
  没有可读解释。现已修复，并给出中文说明。（触发这个报错本身的原因也已修好，
  见下条。）

- **连通性测试失败（报 11-128）**：上一版把系统提示词的默认行为改成「透传
  客户端的 system」后，账号探测请求不再被自动补上 system 首条消息，于是被
  上游拒绝。探测请求现在自带 system，测试恢复正常。

### 改进
- **国际版不再显示签到相关入口**：国际版没有签到体系，此前「全部签到」按钮
  和每个账号的「签到」图标在国际版依然显示，点下去只会得到「已跳过」——现在
  这些入口在国际版不可见，账号页标题也不再提「签到」。
  - 设置页「定时任务」补充说明：签到 / 猫猫旅行 / 活跃上报 / 开学季 / 夜猫
    **只对国内版账号生效**，国际版只有保活照常执行

---

## [1.0.27] - 2026-09-14

### 修复
- **切到国际版添加账号，扫出来的还是国内版二维码**：弹窗标题显示的是国际版，
  但生成的二维码链接指向国内版站点，扫码会加错版本。现已按所选版本生成对应
  的授权链接。

---

## [1.0.26] - 2026-09-14

> **本版有两处默认行为跟着上游一起变了**。若你的用法依赖改动前的效果，
> 需要到设置里手动改回来（下面两条各说明了怎么改）。

### 修复
- **系统提示词默认改为「透传」**：默认不再用网关内置提示词替换客户端发来的
  system，而是原样转发。这是跟随上游的默认值变化。
  - 如果你依赖网关提示词来稳定输出、或想避免模板句被内容审核误判，请到
    **设置 → 系统提示词**把模式改成 `custom`
  - 「自定义提示词文件」只在 `custom` 模式下生效（设置页已注明）

- **出站请求默认对齐官方桌面端**：账号探测等请求现在会带上官方客户端的标识
  头，减少被判定为异常流量的概率。跟随上游的默认值变化。
  - **设置 → 出站标识 → 客户端名称**：留空 = 对齐官方桌面端（新默认）；
    若你之前留空是**有意**想隐藏使用端，请显式填入 `SaaS` 保持原效果

### 新增
- **设置 → 并发与熔断：新增「快过期积分窗口」**：到期时间落在窗口内的积分会
  被标记为「快过期」，选号时优先消耗掉，避免积分白白过期。默认 168 小时
  （一周）；**留空或填 0 即关闭**该优化。

---

## [1.0.25] - 2026-09-14

### 修复
- **减少账号被风控判定为异常的可能**：账号登录、签到、查积分、国际版注册与
  试用包领取这几项操作，请求特征与官方客户端不一致（缺少官方使用的一些标识）。
  现已对齐，降低被风控误伤、进而影响账号的风险。

- **国际版账号的地区归属会填错**：国际版新账号需要先完善注册地区，之前提交的
  地区信息格式不对，可能把归属写歪（表现为聊天报 14017）。现已按正确格式提交。

- **设备风控信息现在会随请求发出**：上游支持用「设备标识」进一步贴近官方
  客户端形态，管理端此前没带。现已支持，并且**重新登录不会把它冲掉**。

- **开学季 / 夜猫任务此前不会真正执行**：这两个任务的脚本之前没被打进上游
  容器镜像，即使到点触发也没有可执行的脚本。上游已修复，更新上游后这两类
  任务会正常出现在「任务记录」里。

---

## [1.0.24] - 2026-09-14

### 安全
- **发布包签名：一键更新强制验签（供应链防护，重要）**：更新器以 root 把
  Release 包直接落盘，而「能发 Release」的门槛比想象中低——能合并 PR 的
  协作者、或被钓鱼的维护者账号都能发版。一旦有人往发布包里塞私货，
  **所有点过更新的部署会被同时控制**（可写 SSH 公钥、定时任务等）。
  现在给发布包加签名，更新器内置公钥、验不过就拒绝安装：
  - 签名由维护者在**本机**用 ssh-keygen 生成（私钥绝不进仓库、也不进 CI，
    因为恶意 PR 能改 CI 步骤把密钥偷走）
  - 更新时先验签、后解压；签名缺失、被篡改、或不匹配都会被中止，包不会落盘
  - 更新器代码所在的 `deploy/` 不再随包自动替换（信任锚不能被更新流程改写），
    如需更新会在日志中提示你手动比对
  - 更新面板显示「已验签」绿色标记；走了跳过开关（`WB_SKIP_SIGNATURE=1`）
    时显示醒目告警
  - 发版签名步骤见 [docs/release-signing.md](docs/release-signing.md)
  > **首次启用需要手动更新一次**：当前已部署的实例还没有验签能力，
  > 请手动更新到本版本；此后的一键更新都会自动验签。

- **修复更新包可写出目标目录之外（严重）**：一键更新会把发布包解压到临时
  目录，但此前的校验存在绕过，恶意的更新包能把文件写到目录**之外**——
  实测可覆盖 SSH 授权文件从而登录你的服务器。现已改为拒绝一切链接成员、
  用严格的路径包含判断，并清掉包内携带的特殊权限位。
  > 触发前提是**更新包的来源被控制**（例如仓库发布权限被窃取）。平时不发作，
  > 一旦发作则是**所有执行过更新的部署同时中招**，故按最高优先级修。

- **修复来源 IP 可被伪造（高）**：服务直接暴露、未经过反向代理时，请求方
  伪造一个来源地址就能绕过**全部 IP 管控**——IP 白/黑名单、密钥的 IP 白名单
  与数量限制、以及登录失败的锁定统统失效。现已只在请求确实来自受信代理时才
  采信转发地址。反代在别的机器上时，用 `WB_TRUSTED_PROXY_CIDRS` 加上它的网段。

- **修复密钥功能不可用（v1.0.23 引入的回归，中）**：上一版给密钥操作加审计时
  漏了一个导入，导致**创建与删除密钥直接报错**。已修复并补测试。

- **`days` 参数加上限**：传入超大数值会让日志/统计接口报错（500），现已钳制。

### 新增
- **新增两类定时任务：开学季与夜猫**（设置 → 定时任务）：上游把这两类从
  宿主机的系统 crontab 迁入了内置调度器，现在可在页面上开关并设置执行时刻
  （默认开学季 12:00、夜猫 01:00）。迁移后不再依赖系统 cron，容器重建也不会丢
  - 它们的执行结果会出现在「任务记录」里（此前日志格式不同，识别不到）

### 修复
- **仪表盘切换版本后仍显示另一版本的数据**：账号总数、积分余额、账号健康快照
  都取自完整账号池，没跟着切换过滤（上一版只给账号页/模型页/任务页做了过滤，
  漏了仪表盘）。现在这些数字都随版本变化，「反代上游」面板里的健康/冷却/禁用
  计数也按版本重算
  - 调用趋势与「今日 Token」是**全局记录**（不按版本拆分），已如实标注
    「含两种版本」，而不是假装已过滤
- **国际版说明写错了**：之前页面上写「国际版没有签到 / 猫猫旅行 / 活跃上报」，
  但上游已放开国际版的**活跃上报**。现改为准确表述：
  国际版没有签到 / 猫猫旅行 / 开学季 / 夜猫，**保活与活跃上报照常执行**
- **账号被禁用时看不出原因**：上游对 `11140 request illegal` 是**硬禁用**
  （到期也不自愈，必须重新授权登录），此前界面统一只显示「已禁用」，
  会让人误以为等一会就好。现在显示「已禁用（需重新登录）」并悬浮提示原因

---

## [1.0.23] - 2026-09-14

### 安全
- **修复管理端提权漏洞（务必尽快升级）**：旧版本的管理接口额外接受一种
  密钥头（`X-API-Key`）作为登录凭据，而这类密钥一旦泄露，就能**直接获得
  管理员权限**——可改密码、加账号、改配置。已彻底移除，管理端只认登录会话。
  > 已泄露的密钥仍写在 `users.json` 里，升级后请按下方「升级后必做」清理。

- **改密码 / 改角色 / 删用户后，相关登录态立即失效**：原先要等到会话自然
  过期（默认 7 天）才失效。现在改动即刻生效，其他用户不受影响。
- **删掉的用户不能再登录**：原先其会话在过期前仍然可用。
- **权限变更立即生效**：原先降权后，旧会话仍按原权限工作。
- **登录失败次数无法再被清除**：攻击者可用大量垃圾账号刷接口，把失败计数
  清空后再暴力猜密码。现已封堵。
- **账号文件损坏时不再自动重置**：原先 `users.json` 读写异常会自动重建，
  导致全部账号与登录态被清空。现在会保留原文件并报错，便于从备份恢复。
- **补充审计日志**：记录登录成功/失败、改密码、增删用户、改安全配置、
  改上游配置、密钥创建与删除，含来源 IP。可在「安全」页查看。
- 其他加固：模型白名单无法再被绕过；接口不再暴露账号池规模；日志无法被
  伪造换行；凭据类操作限管理员。

### 升级后必做（顺序不可颠倒）
1. 在部署目录执行 `python3 deploy/purge_credentials.py`
   —— 立即让**所有**既有登录态失效（含攻击者伪造的），并清掉遗留密钥
2. 重启服务：`systemctl restart workbuddy-web`
3. 用当前密码重新登录，然后**修改管理员密码**
4. 到「设置 → 管理用户」核对账号列表，删掉不是你创建的账号
5. 到「安全」页查看审计日志，确认有无异常登录

### 新增
- **国内版 / 国际版切换**：页面右上角一键切换，全站跟随（账号、模型、测试台、
  任务记录），「添加账号」也会走对应版本。请求日志与用量是全局记录，不随切换过滤
- **国际版支持**：可直接扫码添加国际版（workbuddy.ai）账号，弹窗里选择地区
  完成注册并领取试用额度。
  > 国际版**没有**签到 / 猫猫旅行 / 活跃上报，积分仅来自一次性试用——
  > 这是上游的能力边界，页面上已注明
- **设置页新增「国际版」分组**：可开关国际版路由、自定义其接口地址

### 修复
- 「刷新积分」与单账号积分查询改为仅管理员可用（会真实调用上游，此前任意
  登录用户都能触发）

### 说明
- 升级后**既有账号行为完全不变**（没有标记版本的账号一律按国内版处理）

## [1.0.22] - 2026-09-14
### 安全
- **修复任意文件读取漏洞（严重，务必升级）**：无需登录，构造特制网址即可读取
  服务器上的任意文件——包括 `users.json`（内含登录凭据签名密钥，可据此伪造
  管理员登录）、`.env`、上游配置（含 API 密钥）与账号令牌文件。
  现已封堵，所有静态文件请求都会被限制在网站目录内。

- 账号文件读取接口收紧为白名单（只接受本站自己的账号文件）
- `users.json` 权限收紧为仅属主可读写

### 说明
- 登录会话的签名机制本身没有问题（实测无法通过篡改内容伪造成管理员）；
  失守的原因是密钥文件被上述漏洞读走

## [1.0.21] - 2026-09-14

### 新增
- **模型中心**（底栏「模型」）：把账号可用的模型单列一页，显示名称、上下文
  长度、最大输出与支持的推理档位，支持搜索与筛选
  > 数据取自腾讯接口，因此有**模型全名**和**推理档位**（上游接口不提供这两项）；
  > 取不到时会明确标注来源，不会拿旧数据充数
- **聊天测试台**（底栏「测试台」）：不用先建密钥就能直接试调模型，可选模型与
  思考强度（按该模型实际支持的档位），流式输出、可随时停止，
  每条回答下方标注消耗的积分，右下角显示本次累计
  > 会真实消耗积分，因此仅管理员可用

### 修复
- **任务页筛选后一片空白**：点「余额查询 / 令牌保活 / 自动签到」等没有记录的
  类型后，列表与筛选栏会一起消失，只能刷新页面。现在筛选栏始终保留，
  并在每个类型上直接标出条数（含 0），点之前就知道有没有内容
- **任务页数字自相矛盾**：顶部显示「共 62 条」而列表为空、底部又写「共 0 条」。
  现在所有数字与当前筛选保持一致
- **签到记录看不到上游自动签到**：原先只显示本端触发的手动签到。
  现在本端与上游自动签到合并展示，来源标注清楚
- **测试台点「模型 / 思考强度」没有下拉菜单**：菜单被容器裁掉，看起来像没反应。
  已修复

### 说明
- 上游对签到成功是静默的（只打一行汇总），因此自动签到侧展示的是每轮汇总与
  异常记录，不是逐账号明细

## [1.0.20] - 2026-09-13

### 新增
- **可用模型支持手动重新拉取，并如实标注来源**（设置 → 上游配置 → 可用模型）：
  上游 `/v1/models` 优先用池里**随机一个健康账号**去动态拉取（成功缓存 1 小时），
  **失败则回退到编译进二进制的静态表**（另有 5 分钟负缓存）。这带来两个困惑，
  这次都处理掉：
  - 加「重新拉取」按钮：不必再干等上游那 1 小时缓存，也不会因为「刷新页面」
    看着没变化而以为坏了
  - 来源如实标注：动态时显示「上游实时列表 · N 个」，回退时显示
    **「上游内置回退表（非实时） · N 个」**并给出解释——此前一律写
    「来自上游实时列表」，回退时用户会误以为是自己账号/配置出了问题
  - 判据用上游动态条目才有的 `max_output_tokens` 字段；判不出时不猜，
    回退中性文案（`unknown`）
- **说明模型数量为何因人而异**：列表由上游随机挑一个账号拉取，**取决于该
  账号的授权**（企业 / 套餐），所以「同样的部署、不同账号看到的模型数量不同」
  是上游的正常行为，不是本端少显示。此前有用户反馈「只显示 6 个模型」，
  对照上游源码确认静态表固定为 10 个、其列表是你 16 个的真子集，
  即确为账号授权差异，而非回退或本端缺陷

### 修复
- **「重新拉取」按钮不该被「配置文件可读性」禁用**：模型列表与 config.json
  是否可读是两件事（前者取自上游 `/v1/models`），原先两者耦合会导致
  配置读不到时按钮点不动——恰恰是最需要重试的时候。现只在请求进行中禁用

---

## [1.0.19] - 2026-09-13

### 新增
- **账号列表显示「最后续期」时间**，用来区分「刚被保活续期」和「从没刷新过」：
  界面上那行「有效期」是**剩余时间**，而刷新会把它重新拉满，所以单看天数会
  读反——`7.0 天` 的账号可能刚刚续期、非常健康；`60 天` 的账号反而可能是
  当初扫码签发后**再没被刷新过**，保活没覆盖到，到期就会掉线。
  现在在进度条下方显示「最后续期 N 前」（取 JWT 的 `iat`；刷新会换发新令牌，
  故 iat ≈ 最近一次刷新时刻）。实测三种账号一眼可辨：
  - 刚续期：`7.0 天` + `最后续期 1 小时前`
  - 从未刷新：`5.0 天` + `最后续期 55 天前` ← 真正要留意的
  - 非 JWT：靠文件时间兜底，见下条

### 修复
- **进度条基准的兜底窗口从「一律 60 天」改为按实际推算**：JWT 解不出时
  （扁平形手写 auth 文件的 accessToken 常不是标准 JWT），原先直接假定
  60 天当满格。于是**一个 7 天的令牌进度条只显示 12%**，看着像快过期，
  属于误报。现改为用 auth 文件的 `mtime` 推算——上游刷新 token 后会
  **原子写回**该文件，安装器写盘也走同一路径，故 `exp - mtime` 即本次
  有效期的近似值。实测该场景下进度条从 8.3% 修正为 71.4%。
  依然解不出时保持 `None` 并回退旧的保守窗口，绝不显示负数或误报。

### 说明
- 「最后续期」纯展示、不落库：签发时间从令牌自身解出，刷新换发令牌时会
  自动更新，不需要额外存储，也不会与令牌不一致

---

## [1.0.18] - 2026-09-13

### 新增
- **请求日志新增「首字」列（time-to-first-token）**：原先只有一列「延迟」，
  但那是**整条响应**的耗时——流式请求要等模型把全部内容生成完才结束，所以
  回答越长数字越大，13~24 秒很常见，根本看不出上游响应快慢。
  现在拆成两列：
  - **首字**：从发起上游请求到收到**第一个含正文的 delta**。这才是
    「上游多久开始回话」，判断上游快慢只看它
  - **总耗时**（原「延迟」改名）：含模型生成全程，用来看一条请求的整体开销
  - 非流式请求没有中间过程，首字显示 `—`；详情面板写「未采集（非流式请求）」
  - 阈值也移到首字上：≥3 秒才标琥珀色（总耗时对长回答天然就高，标色只会误报）

### 说明
- 首字采集刻意**不把「收到第一块 SSE」当作首字**：OpenAI 流的第一块通常只有
  `role`、`content` 为空，若以它计时，记的是建连时间，数字偏小且失真。因此
  要等到真正含 `content`（或推理模型的 `reasoning_content`）的 delta 才算
- 数据库新增 `first_token_ms` 列（可空）。已有部署升级时自动 `ALTER TABLE`
  补列，历史记录该列为 `NULL`（显示 `—`），不会误报为 0ms

---

## [1.0.17] - 2026-09-13

### 修复
- **升级后「更新日志」页报「未找到更新日志文件」**：v1.0.16 新增的更新日志页
  只读部署根目录的 `CHANGELOG.md`，但**更新器与安装脚本都不会把该文件放进
  部署目录**——更新器只替换 `server/`、`web/out/`、`deploy/` 与 `.version`，
  安装脚本也只同步这些。于是发布包里虽然有它，装到机器上却没有，页面直接报错。
  更麻烦的是：已经升到 v1.0.16 的部署因为「已是最新版」不会再触发更新，
  点多少次更新都补不上这个文件。现改为：
  - 解析时按优先级找 `根目录/CHANGELOG.md` → `server/CHANGELOG.md`
  - 发布打包时在 `server/CHANGELOG.md` 放一份副本——`server/` 每次更新都会被
    整体替换，这条路**在任何部署形态下都拿得到**
  - 安装脚本同时把 `CHANGELOG.md`、`README.md` 复制到部署目录
  - 报错信息列出已尝试的路径，便于自行排查
- 顺带说明：v1.0.16 里「更新时同步 CHANGELOG.md/README.md」的改动本身没错，
  但它只在**下一次**更新时才生效，救不了当时已经装上的那份；这次的兜底
  副本才是真正覆盖所有情况的做法

---

## [1.0.16] - 2026-09-13

### 安全
- **修复凭据泄露：上游新增的 `upstream.device_token` 会被明文返回给前端**。
  上游本次新增了设备风控 token（X-Device-Token），它相当于把一台可信设备的
  身份借出去，泄露可被他人复用。而本端的配置接口此前只脱敏了 `api_key` 与
  `upstash.token`，新字段被原样下发（实测确认）。
  现与 Upstash token 同样处理：只回传「是否已配置」+ 掩码，前端拿不到明文
- 写入语义区分清楚，避免把凭据误清空或写坏：
  - **留空** = 保持原值（前端回显的是掩码，不能被当成清空）
  - **非空值** = 设为该值
  - **显式 `null`** = 清除该键（否则没有办法删掉它）
  - 拒绝含换行/控制字符、超过 512 字符的值

### 新增
- **更新提示现在能看出「这批更新做了什么」**：原先只显示上游最新一条提交，
  而上游常一次累积多个提交——只显示一条会让人以为「就改了这一处」，
  也判断不出是功能还是修复、值不值得跟。现在：
  - 显示**落后多少个提交**（如「落后 7 个提交」）
  - 可展开查看这批判的**逐条提交说明**（最多 20 条，最新的在前）
  - 提交前缀（`feat` / `fix` / `refactor`）本身就是分类，一眼可辨性质
  - 用 GitHub compare API 取，本地提交不在远端（上游 force-push）时静默降级，
    不影响版本检测本身
- **出站标识配置补全**（设置 → 上游配置 → 出站标识）：上游本次新增了几个可配项，
  本端一并可视化，便于对齐官方客户端、让官网「使用端」显示正确：
  - `upstream.client_name` —— 用量归属头（X-Product / X-IDE-Name / X-IDE-Type）
  - `upstream.client_version` / `upstream.cli_version` —— 出站 UA 的版本段
  - `upstream.device_token_file` —— 设备 token 文件路径（留空不读）
  - `upstream.passthrough_ip` —— 是否透传客户端 IP（默认关闭，不把内网 IP 暴露给上游）
  - 这些字段均做了控制字符与长度校验；同段其他键不受影响
- **管理端内置「更新日志」**（设置 → 更新日志）：此前只有仓库里有一份
  `CHANGELOG.md`，要关掉面板、打开 GitHub 才看得到更新了什么。现在：
  - 直接渲染 `CHANGELOG.md`，**离线可用**（文件随发布包分发，不需要联网）
  - 按版本折叠，默认展开最新一个；当前运行版本标注「当前版本」并可定位
  - 未发布内容单独标为「开发中」，与已发布版本区分开
  - 分类（安全 / 新增 / 修复 / 改进）带配色标签，一眼看出改动性质
  - 不引入 markdown 依赖：解析器只认本项目固定的三级结构，
    行内仅处理 `**加粗**` 与 `` `代码` ``，发布包更小
  - 顺带修正一个隐患：一键更新原先只替换 `server/`、`web/out/` 与
    `.version`，**不含 `CHANGELOG.md`/`README.md`**——若不同步，
    更新后界面里的日志会一直停在旧版内容。现一并同步

---

## [1.0.15] - 2026-09-13

### 修复
- **反代调用报「请求体过大（上限 8 MiB）」，但上游上限其实可调**：
  网关把上限**写死** 8 MiB（注释说「与上游对齐」，可上游的
  `server.max_body_mb` 是可配置的，管理端设置页也能改）。于是它成了隐性瓶颈——
  把上游上限调大后，请求仍在本端先被 413 拦掉，且看不出是谁拦的。
  现改为**跟随上游 `server.max_body_mb`**（默认 8 MiB；读不到配置时回退默认，
  不因读不到而拒绝服务），加 10 秒缓存避免每请求读文件
- **大小校验原先只看 `Content-Length`**：分块传输时该头缺失、也可以伪造，
  因此超大请求体仍会被整段读进内存（正是当初要防的内存耗尽）。现改为
  **读取时逐块累计并即时截断**，缺头与伪造头都拦得住
- 413 的报错改为可直接照做：说明当前上限、怎么压缩、以及去
  「设置 → 上游配置 → 请求上限」（`server.max_body_mb`）调大

---

## [1.0.14] - 2026-09-13

### 修复
- **手机上密钥的复制按钮被挤出弹窗、点不到**：密钥展示用了
  `flex-1 + overflow-x-auto` 但没加 `min-w-0`——flex 子项默认 `min-width:auto`
  不肯收缩，长密钥就把复制按钮推到弹窗外面。现改为可换行显示
  （`min-w-0 break-all`），复制按钮带文字标签且固定在弹窗内
- **添加账号的授权链接无法复制**：弹窗里只有可点击的链接，想把它发给朋友
  让对方扫码授权时没法复制。现新增「复制链接」按钮，并在支持 Web Share API
  的浏览器（多为手机）额外提供「分享」按钮，可直接转发到微信等应用
- **设置页「服务信息」的长路径会溢出卡片**：`truncate` 同样因缺 `min-w-0`
  不生效（账号目录/上游声明目录）。现补上收缩约束，并给账号目录加复制入口

### 改进
- 新增通用 `CopyButton` 组件统一复制交互：自身 `shrink-0` 不被长文本挤掉、
  移动端触控区域 32×32、复制后短暂显示对勾（移动端看不到 toast 时也明确），
  并在日志详情的来源 IP / User-Agent / 错误等长字段上补齐复制入口

---

---

## [1.0.13] - 2026-09-13

### 修复
- **上游更新失败时能看懂原因，并且有退路**：上游某次提交删掉了一批脚本却漏改
  构建配置，导致重建失败——**这是上游自身的问题**，但当时界面只报「命令失败」
  加一串难懂的 checksum 报错，既看不出原因、也没有回退手段。
  现在增强三处：
  - **构建前预检**：解析 `Dockerfile` 的 `COPY`，若引用了仓库中不存在的文件，
    在构建**之前**就明确报出原因并列出缺哪个文件，不再让你对着 docker 的长报错猜
    （只判定确凿情况，跳过阶段拷贝 / 通配符 / URL，不会误报把正常更新挡下来）
  - **失败诊断**：把 docker 报错归纳成一句人话（Dockerfile 缺文件 / 磁盘不足 /
    网络问题 / 权限不足）
  - **说明当前服务状态**：构建失败时 compose 不会动已在运行的容器，
    日志里会明确写出「旧容器未被影响，服务正常」，避免误以为服务已挂

### 新增
- **固定上游版本**（设置 → 系统更新）：可填提交号或标签，之后「更新上游」
  会检出该版本而不跟随分支，用于上游某提交有问题时**一键回退**；
  留空即恢复跟随分支。版本值经严格校验后才会拼进 git 参数

### 改进
- **任务记录改为「固定高度 + 内部滚动」，不再分页**：分页虽能控制高度，
  但与右侧「上游原始日志」的滚动方式不一致，翻页也不如直接滚直观；
  且一页没填满时下方会留一片空白。现三块都用固定高度内滚，页面高度恒定
  （实测 7.5 屏 → 1.3 屏）
  - 单次拉取 200 条（手机上 80 条）以控制 DOM 规模
  - 命中上限时底部明确提示「共 N 条 · 仅显示最近 200 条，更早的请缩小时间范围」，
    不静默截断
  - 两块并排的卡片高度一致（470px），右侧不再有高度不齐的空白
  - **滚动条改为细样式**：浏览器默认滚动条偏宽、带可见轨道，嵌在卡片里很突兀。
    新增 `.scroll-slim`（主题色 30% 透明滑块 + 透明轨道，悬停加深），
    并统一应用到任务记录的三个列表、表格容器与更新日志区
  - **手机上高度按视口自适应**（`calc(100dvh-200px)`）而非套用桌面固定值：
    手机屏更矮，固定高度会明显偏短、卡片下方留一大片空白；
    用 `dvh` 而非 `vh` 是为了在地址栏收起/展开时也算得准

---

## [1.0.12] - 2026-09-13

### 安全
- **升级 Next.js 15.3.8 → 15.5.25**（响应 Issue #2）：15.3.8 落在多个 Next.js
  公告的影响范围内（含 2 个 critical）。npm audit 已从 3 条降至 **0 条**
  - 说明：本项目生产环境是 `output: 'export'` 静态导出，由 FastAPI 托管静态文件，
    **运行时并没有 Next.js 服务端**（无 middleware / route handler / server actions，
    `images.unoptimized: true`），因此那些 SSRF / RCE / DoS 公告在生产上并不适用。
    但公开仓库应当保持依赖干净，且升级成本极低，故一并处理
  - `eslint-config-next` 同步升到 15.5.25
  - 加 `overrides.postcss = 8.5.28`：Next 内置的 postcss 8.4.31 落在公告范围内，
    顶到已修复版本后 audit 归零；已实测构建产物与全部路由正常
  - 未升级到 Next 16：那是破坏性大版本，收益不符合风险

### 新增
- **实际扣费（实付）可视化**：上游 2026-09-13 起在响应末帧的 `usage.credit`
  里返回**本次真实扣费**，管理端现将它与 Token 一起留痕
  - 请求日志新增「实付」列，详情抽屉显示「实际扣费」（`0` 会标注「未计费」）
  - 用量统计新增「今日实付」卡片（附本周合计），按模型 / 按密钥明细表也加了「实付」列
  - **区分「上游未返回」与「扣了 0」**：缺失存 NULL、界面显示 `—`，
    0 才是真的免费。混为一谈会把未知误判成免费号
  - 顺带扩了 `usage_daily.credit`（当日扣费合计），趋势图数据里也有了
- **数据库自动迁移**：新增字段时老数据库会自动补列，可重复执行、保留既有数据，
  升级不再需要手工动库

### 修复
- **任务记录页会随记录数无限变长**：两块列表一次把取回的数据全部渲染，
  实测 500 条任务日志 = **19 屏、9000 个 DOM 节点**，历史一多就要一直往下滑。
  现改为**服务端分页**（每页 20），页面高度不再随数据增长
  （同一份数据：19,229px → 1,615px，DOM 9,003 → 741）
- **列表徽章「共 N 条」与实际可见条数不符**：徽章取的是全局统计，而列表只取
  前 500 条，会出现「徽章说 2200 条、实际只能看到 500 条」且更早的记录翻不到。
  现分页总数取**当前筛选下**的计数，可一直翻到最早一条
- **签到记录占掉半屏多**：行高 39px（结果列的 Badge 有内边距）且与「上游原始
  日志」并排对齐，把右侧只有几行内容的卡片一起撑到 924px，右半屏大片空白。
  现压缩签到行高（39 → 29px）、两块改为**各自独立高度**、并做成 12 条/页

### 改进
- **移动端三块整合成一张卡片 + 分段切换**：手机上三块竖着堆叠、各自带表头与
  分页，页面又长又碎（2.6 屏）。现改为顶部分段（签到记录 / 自动任务 / 原始日志），
  一次只显示一块，页面收敛到 1–1.5 屏；桌面端横向空间足够，仍是三块并列
- **移动端每页条数自适应**：手机是卡片列表（一条约 70px，桌面表格行只有一半），
  任务记录在手机上改为 8 条/页，避免一次拉太长
- 任务记录与签到记录都加了**时间范围筛选**（近 24 小时 / 7 / 30 / 90 天），
  切换类型或时间范围会自动回到第 1 页；概览数字也按同一范围统计，避免对不上

---

### 说明
- 扣费字段来自上游，若上游版本较旧（未返回 `usage.credit`），界面统一显示 `—`，
  属于「无数据显示」而非异常

### 计划中
- **海外版（CodeBuddy 国际版）支持**：受上游限制，暂不可用。上游
  `workbuddy2api` 的出站地址硬编码国内域名（`copilot.tencent.com` /
  `www.codebuddy.cn`），且 `chatBase()` / `billingBase()` 不看账号的
  `domain` 字段（该字段只作为 `X-Domain` 请求头发送，是企业标识而非区域标识）。
  需上游先做成按账号/按域选择 base URL，本端再把自实现的登录/签到/余额地址
  改为可配置。见 [Issue #1](https://github.com/ithtelab/workbuddy-manager/issues/1)

## [1.0.11] - 2026-09-13

### 修复
- **适配上游签到健壮性改造（PR #48）后的日志形态**：上游把签到从「只在失败时
  打日志」改成**成功/已签到也打**，并新增两类非账号日志行。原解析器只认
  `<任务> <uid>: <内容>` 一种形态，会导致三处误判：
  - **「今日已签到」被显示成签到失败**：腾讯把重复签到当业务错误返回
    （code 非 0），但语义上是幂等成功。不特判就会在界面上堆出一片红色失败。
    现对齐上游 `IsAlreadyCheckin` 的判定（已签到 / already），归为正常
  - **凭空多出 `done`、`skipped` 两个「账号」**：每轮汇总行
    `checkin done: total=.. ok=..` 与 `scheduled checkin skipped: ..`
    同样符合 `<任务> <单词>:` 的形态，被当成了 uid。现按形态分别处理：
    汇总行解析成「本轮签到完成：共 N 个，成功 x，已签到 y，失败 z，跳过 w」，
    计划跳过记为一条 info，两者账号列均为空
  - **`checkin <uid> refresh|save: <错误>` 被整行丢弃**：这是上游新增的
    阶段失败行，uid 与阶段名之间是空格而非冒号。现单独识别，能正确记到
    对应账号上（属于真实失败，需要被看到）
  - 另修正：账号被禁用（`连续 N 次 12153 session dead — 禁用`）此前因上游
    只标 `WARN` 而降级为「警告」，但语义是账号已不可用，现统一判为失败
  - uid 识别改用**保留字排除表**而非「必须含数字」：uuid 前 8 位是十六进制，
    存在全为 a-f 的可能（约 0.05%/账号），要求含数字会让那种账号的日志被
    静默丢弃

---

## [1.0.10] - 2026-09-12

### 修复
- **用量统计会把同一次调用算成两天（时区口径不一致）**：写入用量用**本地日期**，
  而「回填用量」用的是 SQLite 的 `date(ts,'unixepoch')` 即 **UTC**。在 UTC+8 的
  服务器上，凌晨 00:00–08:00 的调用本地日比 UTC 日早一天，回填时同一次调用
  被同时算进两天，造成请求数与 Token **重复计数**。
  - 统一取日口径（一律按本地时区，不再混用 UTC）
  - 新增「**重建统计**」（用量页，管理员）：以请求日志为准重建 `usage_daily`，
    用于清理已被污染的历史数据。回填（增量、只能补缺口）删不掉多出来的行，
    所以需要一次重建；界面上明确提示「若请求日志曾被清空，那部分汇总会丢失」
- **配置页的数字上限形同虚设**：`fieldError`/`toWire` 只校验了时刻、时长与文本，
  `kind: 'num'` 的 `min`/`max` **从未参与校验**，输入框上的 max 只是浏览器属性，
  拦不住手输也拦不住直接调接口。实测「每次上报条数」可写入 999 / 100000
  （即每号每天发这么多条对话，直接烧额度）。现补上前后端双重校验：
  前端即时提示并阻止保存，服务端统一区间兜底（含 `activity_report_count` 1–50、
  `max_in_flight` 0–64、`breaker_threshold` 1–100、`max_body_mb` 1–256 等）
- **「全部签到」串行执行，账号多时必然超时**：逐个 `await`，38 个账号串联
  腾讯 RPC 会超过前端 60 秒超时（前端报失败、后端仍在跑，容易重复点击）。
  改为并发执行并限制并发数（默认 5，`WB_CHECKIN_CONCURRENCY` 可调）；
  实测 38 个账号从 11.4s 降到 2.4s
- **密钥有效期在编辑时完全无法修改**：该字段只在「新建」时出现，密钥一旦创建
  就无法续期、也无法取消过期。现编辑时提供「保持不变／从现在起 N 天／永不过期」
  三选一，并显示当前到期时间（默认保持不变，避免打开就保存把有效期重置）
- **有效期进度条恒为满格**：写死 72 小时（3 天）作为满格，而令牌实际有效期约
  60 天，导致「60 天」和「5 天」看起来一样。现从 JWT 的 `iat`/`exp` 解出该令牌
  的真实总时长，按真实比例展示（实测 58.9/60 天 = 98.2%）；解不出时回退到
  60 天兜底窗口，不影响任何鉴权判断

### 改进
- **二维码轮询加防重入**：`setInterval` 每 2 秒发一次轮询且无在途标志，
  单次超过 2 秒就会叠加请求。现在上一轮未返回时跳过本轮
- **安全配置保存失败会回滚**：此前乐观更新后若请求失败，开关仍停在已被点过的
  状态，与实际后端不一致（刷新才暴露）。现在失败即回滚到改动前的值

---

## [1.0.9] - 2026-09-12

### 修复
- **底栏「密钥」入口消失**：底栏把图标分成「分隔线前 / 后」两段渲染，但前段写死了
  `slice(0, 3)`。新增「任务」入口后它变成第 3 项，原第 4 位的「密钥」被静默丢弃
  （第 4 位正是原来的分隔线位置）。现改为**按分隔线实际位置切分**，不再写死数量
- **适配上游日志格式变更（uid 截 8 位 + WARN/ERR 前缀）**：上游 2026-09-12
  起把日志里的 uid 统一截成前 8 位，并给可疑/失败行加 `WARN:` / `ERR:` 前缀。
  - 任务记录的昵称解析改为**按 uid 前缀匹配**（含前缀歧义保护），
    否则更新上游后账号名会全部退回成 uid
  - 解析器识别 `WARN:` / `ERR:` 级别前缀，直接采信上游的级别判定
  - 适配活跃上报改「5 连发」后的 `report N/M ok` / `report N/M: <err>` 日志行，
    并补充中文翻译

### 新增
- **每次上报条数**（`schedule.activity_report_count`，设置 → 上游配置）：
  上游新增该配置，默认 5 条——领养猫前置需 5 次对话，5 条一次刷满；
  填 1 即旧行为（每号每天 1 条点亮连登）。本端做了 >=1 的校验

---

### 修复
- **适配上游日志格式变更（uid 截 8 位 + WARN/ERR 前缀）**：上游 2026-09-12
  起把日志里的 uid 统一截成前 8 位，并给可疑/失败行加 `WARN:` / `ERR:` 前缀。
  - 任务记录的昵称解析改为**按 uid 前缀匹配**（含前缀歧义保护），
    否则更新上游后账号名会全部退回成 uid
  - 解析器识别 `WARN:` / `ERR:` 级别前缀，直接采信上游的级别判定
  - 适配活跃上报改「5 连发」后的 `report N/M ok` / `report N/M: <err>` 日志行，
    并补充中文翻译

### 新增
- **每次上报条数**（`schedule.activity_report_count`，设置 → 上游配置）：
  上游新增该配置，默认 5 条——领养猫前置需 5 次对话，5 条一次刷满；
  填 1 即旧行为（每号每天 1 条点亮连登）。本端做了 >=1 的校验

---

## [1.0.8] - 2026-09-12

### 修复
- **任务结果全是英文，看不懂**：上游日志是英文原文（`claim ok record=12
  reward=100`、`skip (daily limit reached)` 等），直接展示对中文用户不友好。
  现按展示层做中文化（如「领奖成功：第 12 次行程，获得 100 积分」
  「跳过：今日次数已达上限」），**数据库仍保留英文原文**，鼠标悬浮可看原话，
  便于对照上游日志排查；认不出的新文案原样回退，不会丢信息
- **任务记录里的账号显示为 uid 而非昵称**：上游日志只带账号 uid，
  而账号表里是昵称，两边对不上；现按 uid → 昵称映射显示（取不到才回退 uid）
- **「上游签到 / 保活日志」标题与说明过时**：该区块现在也含旅行与活跃日志
  （上一版放开过滤后），标题改为「上游原始日志」，说明也改为
  「签到成功与保活正常是静默的」，并指向结构化的「自动任务与积分记录」

### 改进
- **数据页改为自动刷新，移除多余的「刷新」按钮**：所有数据页按固定间隔自动拉取
  （账号 / 任务 / 仪表盘 30 秒，密钥 / 日志 / 用量 / 安全 60 秒），手动刷新按钮
  因此完全重复 —— 需要时用浏览器自带的刷新即可。
  **标签页切到后台时跳过刷新**，切回来立即补一次：既不后台空转，也不会回来看到旧数据
- **签到记录与任务记录独立成「任务记录」页**：账号一多，这两块堆在账号管理
  下方会把页面拉得很长，往下翻很久才能看到日志。现在移到独立页面
  （底栏新增「任务」入口），账号页只留账号列表，并在下方给出跳转提示；
  新页面提高展示上限（签到 60 条、任务 500 条）且不再内嵌滚动区，
  一页看全，30 秒自动刷新
- **采集器回看窗口从 800 行加到 3000 行**：上游每处理一个请求就打一行访问日志，
  行数消耗很快；窗口太小可能在两次轮询（45 秒）之间漏掉任务行，
  且刚升级部署时只能补到很短一段历史

---

## [1.0.7] - 2026-09-12

### 新增
- **积分变动流水**：所有让余额增加的渠道都会留痕，不再只有旅行领奖可见
  - 上游只在猫猫旅行领奖时打印具体数额（`reward=100` / `+300 credits`），
    签到、活跃上报等**根本不打日志**，所以光解析日志覆盖不全
  - 现在每次真实查询积分后比对上次余额，**只要增加就记一条**「积分变动」
    流水（含 `余额 +100（1300 → 1400）`），从而覆盖全部获取渠道；
    减少是消耗，不计入
  - 余额基线持久化在数据库，重启后继续比对；首次见到的账号只建基线，
    不会把历史余额误报成「刚获得」
  - 「自动任务与积分记录」新增「积分变动」类型筛选
- **积分数据来源可见**：账号积分旁标注 **`实时`** 或 **`缓存 Ns 前`**，
  一眼看出这个数字是刚查的还是 60 秒内复用的缓存，不再把滞后值当成实时值
- **系统提示词模式可配置**（设置 → 上游配置）：上游新增 `prompt.mode`，
  默认 `custom` 会**用网关自带提示词替换客户端传来的 system 消息**。
  现在可在界面上切换：
  - `custom`（替换为网关提示词，默认）
  - `passthrough`（透传客户端 system，保留你自己的角色设定与格式约束）
  另可指定自定义提示词文件路径（`prompt.file`）。
  该分组带显式警示：`custom` 会覆盖下游 system prompt；填了文件路径就必须可读，
  否则上游启动会直接失败
- **请求体上限**（`server.max_body_mb`）与**出站 User-Agent**（`upstream.user_agent`）
  纳入可视化配置——都是上游本次新增、此前界面缺失的字段
- **自动任务与积分记录**（账号页）：猫猫旅行 / 活跃上报 / 自动签到 / 保活
  的执行结果与**积分收益**现在可以查看，不必再登服务器翻容器日志
  - 上游把这些结果打在容器日志里，且**容器一重建（更新上游）日志就没了**。
    现在由后台采集器每 45 秒解析一次新日志并落库（`task_logs` 表），长期保留
  - 解析出类型、账号、积分、以及结果等级（积分 / 正常 / 跳过 / 告警 / 失败），
    旅行领奖（`reward=100`、`adopt ok (+300 credits)`）会显示为 **+100 / +300**
  - 支持按类型筛选，顶部汇总总条数与**累计积分**；提供「立即采集」按钮
    不必等后台轮询
  - 采集用「时间戳 + 内容」做去重键，重复读取同一行不会重复入库
  - 新增接口 `GET /api/task-logs`、`POST /api/task-logs/collect`、
    `POST /api/task-logs/clear`
- 上游日志过滤补齐 `travel` / `activity`，此前这两类日志在「上游签到 / 保活日志」
  里被直接丢掉（只匹配 checkin / keepalive / user-resource）；
  该区域同时不再显示 docker 的重复时间戳前缀

### 修复
- **打开账号页时积分有时不显示**：积分查询与账号列表是两个并发请求，
  积分常常先返回，此时账号列表还是空的，就地更新会落空。
  现改为按 uid 单独保存、渲染时再合并，与请求先后无关

### 改进
- **移动端 UI**：
  - 账号列表在手机上改为**纵向卡片**（昵称/UID/状态/积分/有效期/操作一屏可见），
    桌面端仍用表格；原先 6 列表格在窄屏需要横向拖动才能看全一行
  - 「自动任务与积分记录」同样在手机上改为卡片
  - 修复设置页标签栏在手机上撑破页面（整页横向溢出到 492px），
    现改为可横向滚动
  - 头部按钮在手机上收敛文案（「强制重启」→「重启」、「刷新积分」→「积分」）
- **移除仪表盘多余的「刷新」按钮**：该页每 30 秒自动刷新，且没有任何会
  改变数据的操作，按钮纯属重复；描述里已注明自动刷新
- **设置页布局紧凑化**：分组内的字段改为宽屏两列（开关与它对应的时刻/数值
  天然成对），八行「定时任务」压到四行；去掉每个字段下方重复的格式说明，
  仅在填错时显示；卡片与标签页间距整体收紧

---

## [1.0.6] - 2026-09-11

### 修复
- **已是最新版本却提示「发现新版本可用」，还显示成降级**
  更新到 v1.0.5 后，界面弹出「管理端：v1.0.5 → v1.0.4」。
  根因是版本比较写成了「不相等」而不是「严格更新」：检测结果缓存 6 小时
  （避免触发 GitHub 未认证 API 限流），缓存里存的 `latest` 是更新前查到的
  v1.0.4，与当前版本 v1.0.5 不相等 → 被当成有更新。
  现在改为**数值化比较且必须严格大于当前版本**——
  `1.0.10 > 1.0.9`（按数值而非字符串）、相等不算更新、
  低于当前版本（回滚场景）也不再提示。
- **更新完不清版本缓存**，导致刚发布的新版本最长 6 小时才被发现。
  现在管理端更新成功后清除 `version-check.json`，下一轮检测立即拉取最新。
  即使在 GitHub API 限流、只能沿用旧缓存的极端情况下，
  「严格大于」的比较也保证不会再冒出降级提示。

---

## [1.0.5] - 2026-09-11

### 修复
- **一键更新「管理端」成功后仍显示「更新未完成」**
  更新脚本最后一步是 `systemctl restart workbuddy-web`，而 systemd 默认
  `KillMode=control-group` 会连同同一个 cgroup 内的更新进程一起终止
  （`start_new_session=True` 只脱离终端会话，并未脱离 service 的 cgroup）。
  旧流程把「写入成功状态」放在重启之后，于是状态文件永远停在
  `running=true, ok=null`，管理端一读就判成「更新进程异常中断」——
  代码其实已经换好了。
  现在改为**先落盘终态再重启**，并让管理端在发现「标记运行中但进程已不在」
  时进一步核实：目标版本已就位、或日志已打印「管理端已更新到 X」且与
  当前版本一致，即判定为成功（`更新完成（服务已重启）`）；两者都不满足
  才算真中断。
- **更新后「当前版本」不刷新，界面一直提示「发现新版本可用」**
  发布包里的 `.version` 标记在更新时没有一并替换，且读取顺序是
  「标记优先于代码」，导致刚更新完仍显示旧版本号。
  现在更新会同步替换标记；读取策略调整为**以代码版本为准**，
  标记与代码不一致时自动纠正（兼容旧流程装出来的部署）。

---

## [1.0.4] - 2026-09-11

### 修复
- **配置页把「执行时刻」当成数字间隔写入，会写坏上游配置**
  `schedule` 下的 `checkin_hours` / `travel_hours` / `activity_hours` /
  `keepalive_hours` 在上游是**整点数组**（如 `[9, 21]`），此前界面按
  「每隔几小时」渲染为数字输入框，保存会写成 `"checkin_hours": 6`，
  既非数组也非整点，重新加载后行为不可预期。
  现改为逗号分隔的时刻输入（`9, 21`），带即时校验（0-23、去重、排序）
- **`pool.breaker_cooldown` 字段类型写错**：上游该字段为时长字符串
  （默认 `30m`），此前按秒数整数渲染，保存会把 `"30m"` 覆盖成数字导致
  上游启动报错。现统一为时长输入并做格式校验
- **「更新完成」提示无法关闭**：更新结束后的结果横幅没有关闭入口，
  会一直占据设置页。现右上角增加关闭按钮；发起下一次更新时会重新出现，
  日志区仍保留完整记录
- **`updater_found` 提示在页面初次加载时闪现**：状态尚未拉取（`null`）
  时被误判为「未找到更新脚本」。现仅在确实读到 `false` 时提示

### 新增
- **定时任务可视化配置**：四类任务（签到 / 猫猫旅行 / 活跃上报 / 保活）
  各自独立的开关与执行时刻，对应上游新增的 `travel_*` / `activity_*` 字段
- **功能开关 / 会话粘性配置**：`features.sanitize_blacklist_fingerprints`、
  `session_sticky`（开关、`ttl`、`gc_interval`）纳入可视化编辑
- **并发与熔断补全**：`idle_weight_per_hour` / `idle_weight_max`
  （闲置补偿，支持小数）可直接调节
- 高级 JSON 编辑器扩展为 5 个段：`schedule` / `cooldown` / `pool` /
  `features` / `session_sticky`
- **配置写入服务端兜底校验**：时刻数组必须是 0-23 的整数数组、
  时长必须是 `30s / 10m / 2h / 1d` 形式，非法值直接拒绝并说明原因，
  不会写入上游配置

### 改进
- `cooldown.soft_rate` 默认值跟随上游改为 **600s**（此前上游为 60s）

---

## [1.0.3] - 2026-09-12

### 新增
- **一键更新**（设置 → 系统更新）：网页上直接更新，无需登录服务器
  - **新版本自动检测与提醒**：进入页面即检查管理端 Release 与上游最新提交，
    发现更新会弹出提醒，并在面板显示版本对比（当前 → 最新）与上游最新提交说明，
    附针对性的「更新上游 / 更新管理端」按钮
  - 检测结果缓存 6 小时（GitHub 未认证 API 限流较严），支持手动「检测更新」
  - **全部更新 / 仅上游 / 仅管理端** 三种模式，带二次确认
  - 实时进度：更新过程与日志逐行展示（后台执行，页面自动重连）
  - 账号授权、上游配置、密钥与日志数据全部保留
  - 上游更新会**自动重新施加端口收敛**（127.0.0.1），避免上游仓库里的
    `7863:7863` 覆盖安全基线
  - 安全约束：仅管理员可用；目标为固定枚举，路径取自服务端环境，
    **不接受客户端传入命令或路径**；解压做目录穿越校验；并发更新加锁
- 版本标记：发布包与安装脚本写入 `.version`，界面据此显示当前版本

### 修复
- **用量柱状图在数据点少时被拉伸**：未限制柱宽，只有一两天数据时单根柱子
  会占满整个图表。现限制最大宽度（48px）

### 改进
- **部署大幅简化**：`deploy/install.sh` 升级为一键脚本，自动检测并安装上游
  workbuddy2api（克隆、生成随机 api_key、修正目录属主、构建并启动容器、
  等待就绪），随后安装管理端并注册 systemd 服务。解决「clone 本仓库后
  因缺少上游而无法运行」的问题
  - 已存在上游时自动跳过，不覆盖其配置与账号（含 `--skip-upstream` 参数）
  - 支持 `APP_DIR` / `UPSTREAM_DIR` / `MANAGER_PORT` 等环境变量自定义
  - 含环境预检（Python 版本、Docker、端口占用）与非 root 拒绝
- 新增 [deploy/README.md](deploy/README.md)：完整部署指南
  （Release 包与 git clone 两种方式、Nginx 配置示例、加固建议、常见问题）
- 发布包纳入 `docs/`（此前只含顶层文件，导致缺少安全审查报告）

### 计划中
- 出站 IP 池：为每个腾讯账号绑定独立出口代理（需在上游 workbuddy2api 侧支持）
- 用量统计支持小时级粒度

---
## [1.0.2] - 2026-09-12

### 改进
- **部署大幅简化**：`deploy/install.sh` 升级为一键脚本，自动检测并安装上游
  workbuddy2api（克隆、生成随机 api_key、修正目录属主、构建并启动容器、
  等待就绪），随后安装管理端并注册 systemd 服务。解决「clone 本仓库后
  因缺少上游而无法运行」的问题
  - 已存在上游时自动跳过，不覆盖其配置与账号（含 `--skip-upstream` 参数）
  - 支持 `APP_DIR` / `UPSTREAM_DIR` / `MANAGER_PORT` 等环境变量自定义
  - 含环境预检（Python 版本、Docker、端口占用）与非 root 拒绝
- 新增 [deploy/README.md](deploy/README.md)：完整部署指南
  （Release 包与 git clone 两种方式、Nginx 配置示例、加固建议、常见问题）
- 发布包纳入 `docs/`（此前只含顶层文件，导致缺少安全审查报告）

---
## [1.0.1] - 2026-09-12

### 安全（详见 [安全审查报告](docs/SECURITY-AUDIT.md)）

- **修复高危：伪造 `X-Forwarded-For` 可绕过 IP 管控与登录锁定**
  反向代理以「追加」语义写 XFF，首段完全由请求方控制；原先直接取首值，
  导致入站 IP 白/黑名单、每密钥 IP 限制、登录失败锁定均可被冒充绕过。
  现优先采用反代覆盖写入的 `X-Real-IP`，退化时从 XFF 右侧取值
  （新增 `WB_TRUSTED_PROXY_HOPS` 适配前置 CDN）
- **修复高危：登录爆破仅按 IP 计数**，换 IP 即可无限尝试同一账号。
  现改为「IP + 用户名」双维度锁定（各 5 次 / 10 分钟）
- **修复中危：`/docs`、`/redoc`、`/openapi.json` 对公网暴露**，
  把全部管理接口结构告知未认证访问者。生产环境默认关闭
  （`WB_ENABLE_DOCS=1` 可开启）
- **修复中危：`/api/sysinfo` 未认证即泄露服务器绝对路径**。改为需登录，
  且仅返回服务名与版本
- **修复中危：网关无请求体大小限制**，超大请求会整段读入内存。
  现校验 `Content-Length`，超过 8 MiB 返回 413（与上游限制对齐）
- **修复低危：登录失败计数字典无上限**，可被大量源 IP 撑占内存。
  现设容量上限并清理过期项
- **修复低危：网关无限流**，单密钥被打爆会拖累整个账号池。
  现按密钥做滑动窗口限流（默认 120 次/分钟，可配），超出返回 429
- **新增安全响应头**：`X-Content-Type-Options`、`X-Frame-Options: DENY`、
  `Referrer-Policy`、`Permissions-Policy`、CSP

### 修复
- **积分余额不准确 / 不自动更新**：上游 `/status` 的 `credits` 只在上游自己的
  定时任务里刷新，滞后可达数小时，且手动签到不会带动它更新。现改为管理端
  直接向腾讯查询实时余额（带 60 秒缓存，避免频繁请求）：
  - 账号页打开时自动拉取一次实时积分
  - 签到后立即返回刷新后的积分，并就地更新显示
  - 新增「刷新积分」按钮，可手动拉取全部账号的实时余额
  - 单个账号积分接口支持 `force=true` 绕过缓存

### 改进
- 账号页与仪表盘新增 30 秒自动轮询，避免长时间停留时展示陈旧数据
- 图标 / 字体等静态资源改为缓存一天（此前每次回源校验）

---
## [1.0.0] - 2026-09-11

首个正式版本：为 [`workbuddy2api`](https://github.com/Sliverkiss/workbuddy2api)
提供完整的 Web 管理控制台与 OpenAI 兼容反代网关。

### 新增

**账号管理**
- 扫码纳管腾讯账号（微信 / QQ），授权后自动签到、落盘并重载上游
- 账号列表：Token 有效期进度、状态徽章（在线 / 冷却中 / 已禁用 / 已过期）
- **积分余额查看**：显示各账号当前可花费积分，按余额分级着色
  （≤0 红 / <200 琥珀），未同步时显示 `—` 而非误报 0
- 单账号操作：签到、连通性测试、刷新 Token、删除
- **一键全部签到**，逐账号记录结果

**反代网关（对外 `/v1`）**
- OpenAI 兼容协议，支持流式（SSE）与非流式
- 多密钥分发：每把密钥独立设置有效期、最大 IP 数、IP 白名单、
  模型白名单、Token 配额
- 模型别名映射：把下游习惯的模型名映射到实际模型
- 入站 IP 管控：全局白 / 黑名单（支持 CIDR），白名单模式可仅放行可信来源
- 全量审计：记录密钥、来源 IP、模型、状态码、延迟与 Token 消耗

**签到可追溯**
- **签到记录**：记录由本端触发的签到结果（手动 / 批量 / 添加账号），
  失败标红并显示原因
- **上游签到 / 保活日志**：采集容器日志中的 checkin / keepalive 记录

**可视化设置**
- 签到与保活：开关 + 间隔，附白话说明，无需手改 JSON
- 并发与熔断：单账号并发、失败阈值、冷却时间
- **Upstash（Redis 持久化）配置入口**：地址 + Token + 连通性测试，
  多实例部署时可共享会话粘性与账号状态
- 只提交改动项，不会覆盖未展示的配置；冷门参数保留「高级设置」
- **保存后自动重载上游**（约 0.5 秒，多次改动自动合并为一次重启）

**用量与缓存**
- 用量统计：按天 / 模型 / 密钥多维聚合，含趋势图
- **用量统计修复**：可按请求日志回填历史缺口（幂等）
- 缓存策略分级：静态资源长期强缓存、API 禁止缓存、HTML 走 ETag 校验

**访问控制**
- 用户名 + 密码登录，PBKDF2-SHA256 加盐存储
- 角色分级：`admin` 可读写 / `viewer` 只读
- HttpOnly 签名 Cookie 会话，同 IP 连续失败 5 次锁定 10 分钟

**界面**
- 浮动底栏：可拖拽、磁吸放大，以水平中心为锚点（悬停不漂移）
- 语义配色：有效期与积分按业务含义分级着色
- 卡片式通知：弹簧入场、语义色图标、底部倒计时线
- 深浅色主题，跟随系统

### 修复

- **非流式请求返回 502**：用量统计函数被注释吞掉，且统计层异常会中断
  流式响应收尾（表现为内容已输出但报 `terminated`）。现已恢复函数并为
  日志 / 统计分别加异常兜底，旁路故障不再影响用户请求
- **配置保存不生效**：保存后配置从未真正应用（后台重载任务没能启动）
- **连通性测试恒报 `code=11101`**：探测使用了非流式请求，而上游强制要求
  `stream=true`；同时补齐了上游要求的请求头（X-User-Id / X-Product 等）
- **管理端读不到上游状态**（`/api/status` 恒为不可用）：构造超时时同时传入
  `Timeout` 实例与 `connect` 关键字，httpx 直接抛 AssertionError
- **上游配置接口泄露明文凭据**：接口会返回包含 API Key 与 Upstash Token
  的原始配置对象（前端并未使用）。现改为仅返回掩码
- **弹窗双层边框**：半透明背景叠加遮罩形成明暗分界，且 `border` 与 `ring`
  重复。现使用不透明背景与单一边框
- **账号计数偶发显示 0**：`inView` 在弹窗缩放挂载时误判为不可见
- **切换页面闪现「正在校验登录态」**：登录校验阻塞了页面渲染
- **添加账号弹窗授权链接溢出**：flex 子项默认 `min-width: auto` 导致
  `truncate` 失效
- **设置页无法读取上游配置时**：明确提示原因并锁定保存，避免空配置
  覆盖真实文件

### 变更

- 上游配置从裸 JSON 改为中文可视化表单（原 JSON 编辑移入「高级设置」）
- 设置页移除冗余的「立即重启上游」按钮（保存已自动重载）
- 账号页「重启上游」更名为「强制重启」，明确为异常恢复手段
- 站点图标由 LDC 的标识替换为本项目形象

### 安全

- **清除设计文档中的真实凭据**：文档含服务器 SSH 密码、公网 IP、上游
  API Key 与面板入口路径，已替换为占位符，并通过历史重写从全部提交中移除
- 上游 API Key 与 Upstash Token 不再通过接口下发（仅返回掩码）
- 网关密钥仅存 SHA-256 哈希，明文只在创建时返回一次
- 所有文件操作做路径穿越校验
- `.gitignore` 排除 `users.json`、`data/*.db`、`.env`、账号授权文件

---

## 版本说明

- 本项目**不修改** workbuddy2api 的 Go 代码，管理端与其独立部署
- 由于上游仅在进程启动时读取配置与扫描账号目录（且不处理 SIGHUP），
  配置变更与账号增删需要通过重启容器生效，本项目已将其自动化

---

## 版本对比

- [v1.0.1 → v1.0.2](https://github.com/ithtelab/workbuddy-manager/compare/v1.0.1...v1.0.2)
- [v1.0.0 → v1.0.1](https://github.com/ithtelab/workbuddy-manager/compare/v1.0.0...v1.0.1)
- [全部提交](https://github.com/ithtelab/workbuddy-manager/commits/main)
